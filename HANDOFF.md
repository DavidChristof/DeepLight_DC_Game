# 接手文档 · 《蚀渊拓荒者 Deep-Light》

> **面向**：下一个接手本项目的 AI（Codex / GPT / 其他模型）+ 未来的作者本人。
> **性质**：本文是**唯一入口**。`AGENTS.md` 是给 Codex 自动读取的精简版，两者冲突时以本文为准。
> **最后更新**：2026-09-19（W18-G3 有限远征回放与试玩矩阵）

---

## 0. 60 秒速览

| 项 | 值 |
|---|---|
| **是什么** | 2D 顶视角「永夜殖民经营 + 蚀潮防守」原型。Canvas 2D，一天 400 秒为一循环，目标是让灯塔活过一波波蚀潮 |
| **技术** | **原生 JS ES Modules · 零构建 · 零依赖 · 零 npm · 零测试框架**（这是硬约束，见 §6.2） |
| **体量** | 88 个 `.js`（源码审计 87 个，不含清单自身）≈ **16.7k 行**；最大四个：`main.js` 124KB · `dev/selftest.js` 138KB · `ui/panels.js` 76KB · `systems/render.js` 66KB |
| **跑法** | 根目录 `py -3.12 serve.py 8000` → <http://localhost:8000> |
| **自检** | 浏览器控制台 `__check()` → 必须 `{ok:true, ran:78, fails:[]}` |
| **当前状态** | **能完整玩通**（开局 → 建家 → 蚀潮防守 → 深渊下潜 → Boss → 第 7 天大潮）。W13 优化、W14-A 战斗轴第 0–8 步、W15-B 生存轴与 W15-C 生态 E0/E1/E2/E3 已落地并完成回归；表面区块出口为固定四格通道、旧区块载入会补齐，边缘切换已加缓冲，夜间弱光边缘可正常刷怪；接近出口走廊时显示方向箭头引导；拓荒者无可执行目标时会原地待命，不再误走地图角落；仓储满载报警采用 4 秒滞回，不会被自动出库重新触发循环；外部循环音乐增加播放看门狗，主菜单首曲在点击手势内直接启动；返回主菜单时即使游戏曲缺失也能稳定切回标题曲，不会在交叉淡化收尾误将标题曲静音；W16-D 拓荒者轴 N0/N1/N2/N3 已接入稳定身份卡、O 键调度面板、工作区/前哨/夜班/救援指令、职业/性格效果集中表、行为影响观测与旧档迁移；N4a 加入 24 秒倒地窗口、现场救援与救援中断，N4b 加入铺位护送、NPC 可站格寻路与 45 秒恢复期，N4c/N4d 加入医疗站、职业照护意图，N5a/N5b 加入死亡履历、关系记忆与单次受限复苏，N6a/N6b-1 加入前哨意图、计时跨区迁移、可中断撤回与三活跃区块闸门，N6b-2a 加入有人远端区块低频采掘、目标容器入库与结算观测，N6b-2b 加入远端食物补给与守灯燃料消耗，N6b-2c 加入远端光压与蚀潮前线记账（不额外刷怪），N6b-2d 加入有限远端求援告警，N6b-2e 加入进入告警区块后的既有倒地/救援兑现（不离屏扣血），默认自动模式固定种子回放曲线保持不变 |
| **版本控制** | ✅ **git 仓库已初始化并推送 GitHub**（分支 `main`，远端 `DeepLight_DC_Game`） |
| **W16-D 最新增量** | N7 完整 E2E 已完成：招募→职业→前哨→救援/死亡→关系→复苏→存读，以及守家/远征/静默十日回放通过；标准回放基线不变 |
| **W16-D N6b-2e** | 已完成：进入告警区块后只兑现一条待处理告警，关联拓荒者进入既有倒地窗口；玩家/NPC 救援、护送、恢复期、死亡与存读沿用原状态机 |
| **W16-D N7a** | 已完成：地图拓荒者显示稳定个人首字与职业色板标记，倒地/护送状态兼容，视觉色板集中在 `VISUAL.colonist` |
| **W16-D N7b** | 已完成：身份卡保存最多 12 条纯数据经历；既有入口写入并去重，O 面板显示最近三条，`__crew()` 与存读投影一致 |
| **W16-D N7 E2E** | 已完成：修复新局建人、死亡清理、读档恢复与远端竖井归属；招募→前哨→救援/死亡→关系→复苏→存读、十日三策略回放和性能门槛均通过 |
| **你的第一步** | W18-G0/G1/G2/G3 已完成；W18-G 首轮闭环收口，后续另立内容打磨小步。W17-F/W16-E/W16-D 已封存，不把历史验收重新当作待办 |

### 0.1 三条命令先跑通（Windows PowerShell）

```powershell
cd d:\26Program\DC_Game
Start-Process py -ArgumentList "-3.12","serve.py","8000" -WindowStyle Hidden -PassThru
# 浏览器打开 http://localhost:8000 ，按 F12 在控制台里：
__check()                    # 期望 {ok:true, ran:78, fails:[]}
__check({roundtrip:true})    # 额外真跑一遍「存档→读档→恢复建筑」
__srcCheck()                 # 异步源码审计，期望 {ok:true, files:87, fails:[]}
```

> ⚠️ **绝对不要用 `python serve.py`**：这台机器 PATH 上的 `python` 是 LilyPond 自带的嵌入式解释器（**没有 `_socket` 模块**），服务器起不来。必须 `py -3.12`。
> 服务器若"端口在监听却连不上"，多半是进程已死残留 LISTEN —— 见 §1.6 诊断口诀。

### 0.2 这个游戏要做成什么（口味定位）

> 原 `GDD.md`（v0.1 立项基线）已于 2026-09-15 归档删除 —— 它的数值几乎全部过时（当时**一天 75 秒**，
> 现在 400 秒），留着只会误导后来的接手者；其中世界观与机制描述早已并入 `GUIDE.md`，深化设计在 `DESIGN_V2.md`。
> 下面这一段是它唯一不可替代的东西：**口味定位**。

**血统**：环世界的殖民地经营 × 星露谷的节奏 × 泰拉瑞亚的探索成长 × 明日方舟式对策防守 × Outer Wilds 的「知识即进度」。

| 支柱 | 落地形态 |
|---|---|
| 经营 / 建造 | 白天采集 → 精炼 → 建造 → 生产，资源多级管线 |
| 防守压力 | 蚀潮昼夜涨落 + 大潮事件，强度逐日爬升 |
| 探索 / 知识解锁 | 新矿物 / 蚀兽档案 / 遗迹图谱**解锁配方**，知识即进度 |
| 构筑 / 策略 | 拓荒者技能构筑 + 对策式防守（**读懂波次再布防**） |

**约束**：不做"什么都有一点但都浅"的大杂烩 —— 四个支柱全部挂在同一条核心循环的上下游，互相喂资源。

---

## 1. 基本配置

### 1.1 环境

- **Windows 10/11 + PowerShell**（所有命令都是 PowerShell 语法，`;` 串命令，不要 `&&`）
- **Python 3.12**：只用来跑 30 行的静态服务器；**游戏本身不需要 Python**
- 浏览器：Chrome / Edge（用 DevTools 控制台跑自检）
- **没有** npm / node_modules / 打包器 / 测试框架 —— 见 §6.2 硬约束

### 1.2 目录结构

```
index.html          唯一 HTML（<canvas> + HUD/面板的 DOM 骨架）
serve.py            本地静态服务器（no-store 禁缓存 + 音频 MIME + 多线程）
css/style.css
js/                 全部逻辑，7 个子目录（见 §2）
assets/             素材：manifest.json + sprites/ + sfx/ + music/ + ambient/
                    多数是占位 —— 画面靠程序绘制、声音靠 WebAudio 合成
docs/               计划与台账（见 §7）
```

### 1.3 本地存储键（改存档格式前必看）

| localStorage 键 | 内容 |
|---|---|
| `deep-light-saves-v2` | 存档：`{ auto, s1, s2, s3 }`（3 手动槽 + 1 自动槽） |
| `deep-light-settings-v1` | 设置（画面/控制/音频/**autosave**），改键映射也在里面 |
| `deep-light-ui-v1` | 侧栏折叠状态 |

旧版单槽存档（`LEGACY` 键）首次运行自动迁移。**存档格式是向后兼容的**：加字段必须给旧档兜底默认值（`js/core/save.js` 里有大量这种注释）。

### 1.4 地图与时间常量（改数值先看这两个文件）

- `js/core/state.js`：`TILE = 16` · `VIEW_W = 720` · `VIEW_H = 480`（内部固定分辨率，CSS 缩放适配窗口）
- 地表地图：`genMap(96, 72, seed)`（`js/main.js`）→ **96×72 格**，同种子 = 同地图
- `js/core/time.js`：**一天 400 秒** = 白天 300 + 黄昏 20 + 蚀潮 60 + 黎明 20
  - `DAY_END 300` / `TIDE_START 320` / `TIDE_END 380` / `DAY_SECS 400` / `DAWN_T 390`
  - ⚠️ 历史文档里的「75 秒/天」「140 秒/天」都是**过时数字**，以这个文件为准

### 1.5 四层世界（`js/data/layers.js`）

| 层 | 层 id | 尺寸 | 法则 |
|---|---|---|---|
| 地表 | `surface` | 96×72 | 昼夜循环 + 太阳光也算光 |
| 余烬层 | `depth1` | 88×64 | 光每格衰减 ×1.6 · 照明半径 −10% —— 燃料是咽喉 |
| 母脉层 | `depth2` | 80×58 | 照明半径 ×0.5 + **回声视觉**（只有走动时才显形）；**畏光盲蚀兽：点亮即武器** |
| 熔渊之心 | `depth3` | 76×56 | 唯一光源是岩浆（免费，但会烧掉生命与空间 —— 踩上去 15 HP/s） |

- 下线：研究「深潜学」→ 造「深潜竖井」→ 站在井边按 `E`
- **新增一层只需在 `LAYER_ORDER` / `LAYER_META` 里登记**（各层 `ore/relic/mother/guards/w/h` 密度也在这里）

### 1.6 开发服务器排障

- 症状「端口 8000 在 LISTEN 但浏览器连不上」= **进程卡死/已死**（历史根因：单线程 `TCPServer` 被浏览器刷新打断后整个 `serve_forever()` 被掀掉；现已改成多线程 + daemon_threads）
- 诊断：`Get-NetTCPConnection -LocalPort 8000 -State Listen` → `Stop-Process -Id <pid>` → 重启

### 1.7 调试句柄：`window.__*`（最重要的工作台）

`js/main.js` 末尾挂了 **80+ 个 `window.__*`** 句柄，**本项目的测试体系完全建立在这上面**。分类速查：

| 类别 | 句柄 |
|---|---|
| 时间/推进 | `__state` `__steps(n, dt)`（**暂停下确定性推进 n 个模拟步**，测试主力）`__step` `__pause` `__quick` |
| 世界 | `__place` `__demolish` `__upgrade` `__finish` `__goto` `__path` `__resolve` `__hoverInfo` `__lamp` `__undo` |
| 资源 | `__give` `__take` `__store` `__spawn(kind,x,y)` `__recruit` `__fire` |
| 战斗观测台 | `__combat()` 全部战斗数值+乘子 · `__wave()` 波次状态 · `__dps()` 理论 DPS · `__lab()` **穷举全部载荷组合** · `__abilities()` 四种敌人行为的现场计数 · `__forecast()` 今晚威胁预报 |
| 系统 | `__blight` `__mind` `__graves` `__relics` `__knowledge` `__research` `__perf` `__stress` `__overlap` |
| **检测器** | `__check(opts)` `__watch(on, every)` `__only(ids)` `__baseline()` `__testLog()` `__srcCheck()` |
| **回放台** | `__replay({ seed, diff, days, policy })` —— 固定种子 + 固定打法跑 N 天，输出曲线表 |
| **真输入** | `__click(sx,sy)` `__rclick` `__move` `__screenOf(tx,ty)`（瓦格坐标 → 真实屏幕像素，配 Playwright 打真键鼠） |
| 存档 | `__saveSlot` `__loadSlot` `__reload` `__wipe` `__newGame(seed, diff)` `__settings` |

> 完整清单（88 个）随时可自己列出：`Object.keys(window).filter((k) => k.startsWith('__')).sort()`。
> 每个句柄的用途都在 `js/main.js` 末尾有中文注释。

**用法示例**（控制台）：
```js
__check()                                  // 全量自检
__check({ roundtrip: true })               // 额外验证存档往返
__watch(true, 20)                          // 长跑期间每 20 帧体检一次 → 玩一局 → __testLog() 看累积违例
__only('tower.level')                      // 只跑指定断言
__replay({ seed: 4242, diff: 'normal', days: 3, policy: 'home' })   // 难度曲线
__give('ore', 100); __place('lamp', 48, 36)                          // 造场景
```

---

## 2. 代码地图

```
js/core/        引擎层（无游戏逻辑依赖）
  state.js      全局 state 对象 + TILE/VIEW_W/VIEW_H 常量
  time.js       一天四阶段时钟（400s）
  save.js       localStorage 存档（槽位/迁移/序列化）
  settings.js   设置持久化      ambient.js  环境光（昼夜）
  audio.js music.js sfxbank.js  音频引擎（合成垫音 + 文件通道，缺文件自动降级）
  assets.js     素材到货检测     perf.js    分阶段性能计时

js/data/        数据表（**唯一数值来源**，不许在 systems/ 里写魔数）
  combat.js     ★ 战斗数值总表（波次/Boss/类型/抗性/技能/载荷/撤退）
  enemies.js    敌人种类表     buildings.js 建筑表（成本/hp/工期/塔等级）
  expedition.js 远征/前哨观测契约、G1 引导与 G2 准备度/G3 回放边界（W18-G0/G3）
  payload.js    ★ 载荷（修饰器）规则 + **唯一的组合数值函数** payloadStats()
  night.js      ★ 一夜三段 + 主题夜 + 刷怪节奏（spawnInterval 是单一来源）
  research.js   研究树（四种东西：数值 Mod / 机制锁 / 知识锁 / 非货币前置）
  difficulty.js calm/normal/harsh 三档
  traits.js     专长短处 + 心志 + **STARVE 饥饿缓冲**
  nightops.js   夜间指令表（夜采/守卫/巡逻）
  ecology.js    生态轴数据（群系/前线/斑块/前哨上限）
  colonists.js  拓荒者身份卡、职业/性格效果与解释数据（W16-D N0/N3）
  tasks.js      任务板任务类型、优先级、可序列化目标与拓荒者调度指令（W16-D N1/N2）
  firstSlice.js 首局垂直切片阶段契约、指标与存档归一化（W17-F0/F3）
  tools.js fire.js storage.js nodes.js relics.js layers.js palette.js sprites.js sfx.js music.js codex.js keymap.js

js/world/       地图与光
  gen.js        种子地图生成（mulberry32，导出给竖井共用）  map.js  瓦格容器
  light.js      多光源 BFS 光照（**光 = 生命线，改这里要特别小心**）
  pathfinding.js BFS + 路径平滑（string pulling，禁斜穿墙角）

js/entities/    player（在 main.js）/ worker.js（拓荒者 AI，36KB）/ enemy.js（蚀兽+Boss，19KB）/ colonist.js / entity.js

js/systems/     按帧推进的系统
  waves.js      波次生成（只在黑暗里刷怪）    combat.js  applyDamage 统一入口 + handleDeath
  towers.js     塔索敌开火（需光照；读 payloadStats）  building.js 工地/工期/等级/载荷
  blight.js     蚀痕前线（局部成长/净化）    ecology.js 生态斑块与中性生物（W15-C E3）
  systems/firstSlice.js 首日软引导、第一潮、余波与远征事件写入（W17-F1/F3）
  expedition.js   远征 HUD、回营方向、准备度状态卡与满仓提示（W18-G1/G2）
  seal.js       封灯撤退（W14-A 第 7 步）
  carry.js      结构装载体（把塔背起来，W14-A 第 5 步）
  nightops.js   夜行指令（夜采/守卫/巡逻）    mind.js   心志/羁绊/墓碑/蚀化
  interact.js   E 键交互总入口（**交互优先级**在这里，多系统抢输入的老窝）
  taskBoard.js  拓荒者任务投影、预约键、调度指令与冲突观测（W16-D N1/N2；不接管寻路）
  storage.js logistics.js craft.js smelt.js tools.js farm.js hazard.js relics.js
  research.js codex.js collide.js hints.js input.js layer.js minimap.js render.js

js/ui/          HUD 与面板
  panels.js     建造/研究/图鉴/容器/载荷/拓荒队 面板（76KB，最大的 UI 文件）
  screens.js    主菜单/新游戏/存档/设置/暂停 全屏界面
  inspect.js    悬停/名册/详情     icons.js  程序绘制图标

js/dev/         **开发工具层（生产代码不依赖它）**
  selftest.js   ★ 不变量检测器：78 项断言 + 87 文件源码审计（138KB）
  observe.js    战斗观测台（__combat/__wave/__dps/__lab/__abilities）
  replay.js     ★ 标准局回放台：确定性 RNG + 政策 + 曲线表；G3 远征策略为一次相邻区块往返
  filelist.js   源文件清单（@generated，供 __srcCheck fetch 自己；新增文件要手工加进去）
```

---

## 3. 验证纪律（本项目最大的资产 —— 别绕过它）

> 这个项目之前"小 bug 太多，得一个一个找"（作者原话），于是把**人工找 bug 换成了机器找 bug**。
> 下面这套流程是它至今还能改得动的原因。**任何改动都要跑，并且要把数字写进台账。**

### 3.1 四层验证

| 层 | 命令 | 期望 | 抓什么 |
|---|---|---|---|
| **运行时断言** | `__check()` | `{ok:true, ran:78, fails:[]}` | 不变量：账本守恒、上限、null 语义、数据表交叉一致、派生数据同步、拓荒者身份与任务边界、首局切片状态、远征观测契约 |
| **存档往返** | `__check({roundtrip:true})` | 同上 | 「改了内存没改序列化」这类静默丢字段 |
| **源码审计** | `__srcCheck()` | `{ok:true, files:87, fails:[]}` | 用了没 import 的符号、`sfx()`/`sprite()` 拼错键、`maybeHint` 键不在表里 |
| **真实键鼠 E2E** | Playwright 打真事件 | 手点能走通 | 只有真输入才暴露的：面板焦点、拖拽、改键、右键误触 |
| **长跑体检** | `__watch(true,20)` + 玩一局 → `__testLog()` | 0 类违例 | 只在特定世界状态下才出现的（见 §6.1 第一条） |
| **难度曲线** | `__replay({seed,diff,days,policy})` | 见 §4.3 | 平衡改动是不是"感觉好多了"—— 要数字 |

### 3.2 ⚠️ 四种局面都要跑（血的教训，复发过 3 次）

`__check()` 在**主菜单/新局**跑时，场上没有灯柱、没有储物箱、没有蚀痕数组 —— **有些断言根本没被碰到，于是"空跑报绿"**（台账里的 D48 / D51 / D56）。

**硬规矩**：新断言不许依赖"游戏恰好走到那一步"（该造数据就自己造，如 `ensureBlight()`）；验绿必须在**四种局面各跑一次**：

1. 主菜单（未开局）
2. 新开一局
3. `__check({roundtrip:true})` 读档往返之后
4. `__replay()` 跑过若干天之后（此时世界里有蚀痕/灯/塔/尸体）

### 3.3 存档纪律（**如果你用浏览器自动化测试，这条最重要**）

- 测试期间 `settings.autosave` **全程保持 `false`**
- **每次 `page.reload()` 之后的第一条脚本里就要重新关掉它** —— reload 会从 localStorage 把 `true` 读回来（这条被漏过 3 次，其中一次真的污染了玩家存档：探测局被写进了自动槽）
- 只要本机 localStorage 里**有**存档（哪怕看起来不是玩家的），先做全量备份 + 逐字节核对，收尾时报告并清理

### 3.4 一步一验，不要攒

**一步只改一个可观测的东西**，改完立刻：`__check` + `__srcCheck` + 相关实测数字 → 写进台账 → **再动下一步**。
禁止"同一批里混新功能 + 修 bug"。

### 3.5 ⚠️ 如果接手者打不开浏览器（纯 CLI 的编码代理常见）

**本项目没有命令行测试入口** —— 78 项断言全部跑在浏览器控制台里，这是刻意选择（零依赖的代价）。三条路径，按推荐顺序：

1. **请作者代跑**（最省事、最可靠）：把要验证的场景写成一小段控制台脚本（照着 §1.7 的句柄），作者贴回结果。
2. **一次性 Playwright**（不改动仓库）：临时写个脚本连本地 `localhost:8000` 执行 `__check()` / `__replay()`。
   ⚠️ **不要把 `node_modules` / `package.json` 提交进仓库** —— 会破坏"零依赖"这条硬约束。
3. **纯静态审查**（最低限度）：至少反向检查改动的所有调用方。原生 ES Module 没有类型检查，**这一步不能替代断言** —— 历史上"看起来对"的改动翻过三次车。

> **没跑过 `__check` 的改动，一律视为未验证。** 这一条不接受"我审过了"作为替代。

---

## 4. 当前进度

### 4.1 已完成（可玩）

**W1–W12：** 引擎（固定步长/多光源 BFS/路径平滑/多层世界/多槽存档）· 昼夜与蚀潮 · 波次与 Boss · 采集建造炼油农田 · 拓荒者 AI 与夜间指令 · 光经济（亮度档/光压/蚀痕）· 夜行（夜辉草/潮穴/夜髓）· 人（专长/心志四级/羁绊/墓碑/招募/蚀化安抚）· 四层法则 · 光路（棱镜接力 + 诱饵灯）· 建造地基（工地/工期）· 存储 L2（容器化库存 + 背包跨层）· 操作台（制造台 + 四种工具 + 岩壁开采）· 自动熔炉与配方化 · 火种可配置 · 研究重排与知识锁 · 完整菜单流程

**W13（优化大工程，全部落地 ✅）：** 操作手感 · 文案与信息架构 · 性能与算法（帧预算 16.6ms）· 音效系统（合成）· 视觉与图标 · 图片素材管线 · 节奏拉长（75→140→400s/天）· 声音文件通道 · 操作三修 + 首次提示

**W14-A 战斗轴（第 0–8 步全部完成并验收 ✅，2026-09-13 ~ 09-15）：**

| 步 | 做了什么 | 关键证据 |
|---|---|---|
| 0 | 战斗数值中央化 + `dev/observe.js` 观测台 | 数值逐项对账；`__wave()` 公式自洽 |
| 1 | **伤害类型与抗性**（图鉴那句"克制"才真正存在） | 光爆打盲蚀兽 1.5×（2 发→1 发） |
| 2a–2e | **载荷框架**（塔 = 发射器 + ≤3 修饰器；`__lab()` 穷举 66 组合）+ 装配 UI + 连锁光塔 | 单靶 DPS ∈ [0.3×, 6×] 基准 |
| 3 | 4 种新敌人行为（吐蚀/冲锋/自爆/光环） | `__abilities()` 现场计数 |
| 4 | **一夜三段 + 主题夜**（壳潮夜/蛾潮夜…） | `spawnInterval` 单一来源 |
| 5 | 手持开火 + **结构装载体**（把塔背起来） | 六处回归（占格/挡光/光照/寻路/存档/跨层） |
| 6 | 塔等级 Lv1–3 + 光路炮塔（prismGun） | 反向断言 `tower.level` |
| 7 | 威胁预报（还剩几波）+ **封灯撤退**（按住 Y）+ Boss 二阶段 | 阶段在 49.4% 切换；封印后 t=390 |
| 8 | **难度曲线台** + 四项有证据的平衡调整 | 见 §4.3 |

**W15-B 生存轴（第 0–7 步已落地 🟡，2026-09-17）：** 生存常量已归入 `data/survival.js`，新增 `__survival()` / `__expedition()`、玩家饱食/伤势、`Z` 口粮与热食；热食靠营地火或点燃炉子，全部资源仍走账本。第 2 步新增简易铺位（1 休整位）、玩家 `U`/铺位 `E` 休整、夜间 `rest` 指令，以及 0–2 层透支对次日建造/采集效率的影响；第 3–6 步完成遗落包、工事维护与受控再生；第 7 步加入确定性相邻区块、活跃/休眠、人工光弱边缘刷怪，以及区块差异存档。B54 已将营地火与潮穴列为不可建造的固定玩法点，回放也复用这一规则。新区块规则下标准三日回放（seed 4242、normal、home）为死亡 0、最低 HP 100、击杀 36、3 级蚀痕 1973；浏览器四局面断言与源码审计均已通过。

**W16-D 拓荒者轴（N0–N3、N4a、N4b、N4c、N4d、N5a、N5b、N6a、N6b-1、N6b-2a、N6b-2b、N6b-2c、N6b-2d、N6b-2e、N7a、N7b、N7 E2E 已完成 ✅，2026-09-18）：** N0 建立稳定身份卡与 `__crew()`；N1 将旧 AI 决策投影为可序列化任务板并观测预约冲突；N2 新增 `O` 拓荒队调度面板，支持模式、工作区、驻守、夜班许可和救援优先级；N3 将职业/擅长/性格效果集中到 `colonists.js`，接入采集、施工、炼油、添火、夜班、心志、移动和蚀巢回避节点；N4a 接入倒地窗口、玩家/优先救援拓荒者、光照与蚀兽中断、食物/燃料代价、存读和名册/地图标记；N4b 在稳定救回后接入铺位护送、NPC 相邻可站格寻路、护送超时/中断与 45 秒恢复期；N4c 接入研究解锁的医疗站、人工光约束、单站单治疗位、全队最多 2 人队列、燃料代价、治疗完成清伤与旧档兼容；N4d 接入名册职业分配、行医治疗倍率、治疗优先/救援优先/仅守灯三种照护意图、候诊重排与救援候选重排；N5a 接入有限死亡履历、墓碑关联、羁绊受影响快照、名册离去记录与旧档兼容；N5b 接入「余烬回声」研究、单次资源复苏、伤势/饥饿/恢复期代价、墓碑去重和存读兼容；N6a 接入守灯/采掘/静默三种前哨意图、当前/目标区块投影和活跃区块容量观测，不改变默认 AI；N6b-1 接入“派往此区块”的计时跨区迁移、可中断撤回、抵达落点与三活跃区块闸门；N6b-2a 接入远端低频采掘与当地容器入库；N6b-2b 接入当地食物补给与守灯燃料；N6b-2c 接入远端光压与蚀潮前线记账；N6b-2d/N6b-2e 接入有限求援告警与进入区块后既有救援兑现；N7a/N7b 接入地图身份标记、个人经历记录与 O 面板投影；N7 E2E 完成招募→职业→前哨→救援/死亡→关系→复苏→存读，以及守家/远征/静默十日回放。默认自动模式固定种子回放仍保持原基线。

**W17-F 首局垂直切片（F0/F1/F2/F3/F4 已完成 ✅，2026-09-19）：** F0 建立 `firstSlice.js` 阶段数据契约（wake→complete）、新局默认状态、存档/读档归一化、`__slice()` 只读报告和回放隔离；F1 接入真实采集与灯柱建成事件，提供 `wake→gather→light` 的非阻塞软引导；F2 接入黄昏/蚀潮/余波的一次性提示、有效灯数与锁夜光压观测、蚀痕复盘指标；F3 接入余波后的首次真实相邻区块往返，记录离营区块与回营日期，并补齐阶段顺序断言；F4 用固定种子完成守家、远征、前哨三路径试玩矩阵，并把光压/挑战/区块字段纳入回放行。首局切片不改资源、波次、区块生成或战斗曲线。后续若调整远征策略或前哨补给，另立小步计划，详见 [`docs/FIRST_SLICE.md`](docs/FIRST_SLICE.md)。

**W18-G 远征与前哨补给闭环（G0/G1/G2/G3 已完成 ✅，2026-09-19）：** G0 新增 [`docs/EXPEDITION_OUTPOST.md`](docs/EXPEDITION_OUTPOST.md)、`data/expedition.js` 与 `__expedition()` 观测契约，报告背包中的储物箱/灯柱准备度、当地容器与人工光、前哨阻塞原因和回营方向；G1 新增当前区块只读远征摘要、返营方向/黄昏倒计时/本地补给 HUD 卡，以及按区块去重的建箱、点灯、补给软提示；G2 新增 `outpostReadinessOf()` 同源准备度、HUD/背包/O 面板五步状态卡与动态刷新，完成真实远端建箱→点灯→补给→派驻→返程后离屏有限产出验证；G3 将开发回放远征策略收束为一次相邻区块往返，输出路线边界字段，并完成守家/有限远征/现场落箱驻守固定种子五日矩阵。四步均不增加跨区自动运输、不改敌潮预算；W18-G 首轮闭环收口，后续另立内容打磨小步。

N6b-2d 已在上述 N6b-2c 记录之后完成：远端告警最多 3 条、同类合并、随区块存读。N6b-2e 已完成进入目标区块后的单次兑现与既有救援状态机接管，未增加离屏伤害或第二套救援。N7a/N7b 已完成地图身份标记与个人经历记录；N7 E2E 已完成全链路与三策略十日回放，视觉和经历只读身份卡/事件数据，不改变默认回放曲线。

### 4.2 回归基线（当前口径，改动前先跑一遍留底）

N6b-2a 已接入：远端驻守者按 12 秒节拍低频采掘，产出只进入目标区块容器；无人区块冻结，守灯/静默撤离不产出。详见 `docs/COLONISTS.md` 的 N6b-2a 实测记录。

N6b-2b 已接入：驻守者饥饿从当地容器补给，守灯每人每轮消耗 1 燃料；缺食物/燃料只暂停本轮并报告原因，暂不绕过现有倒地与蚀潮系统。

N6b-2c 已接入：远端活跃区块每 12 秒只汇总点燃建筑光压；潮夜低光累积有限前线债务，达到阈值后创建/强化确定性 `outpost-tide` 前线，不额外刷远端敌人；字段随区块存档。

N6b-2d 已接入：远端前线事件仅在既有结算脉冲生成最多 3 条可存档告警，同类未处理事件合并；O 面板与 `__outposts()` 可读，且不在离屏状态扣血、判死或开启第二套救援。

W17-F2 已接入：首局切片在黄昏记录有效人工灯数，在蚀潮边界读取既有锁夜光压/挑战系数，在黎明记录蚀痕是否留下；三段各只触发一次，提示沿用现有横幅，`metrics` 随存档保存。该层只做首局观测，不改波次预算、蚀痕成长或资源曲线。

W17-F3 已接入：余波后玩家第一次离开原点地表区块记录 `expedition`，返回 `(0,0)` 记录 `complete`；只认玩家真实跨区，不认拓荒者迁移或深潜层切换，坐标/日期随存档保存。

W17-F4 已完成：固定种子 `4242/normal/5 天` 的守家、远征与前哨路径均有可复现实测；守家最高光压 18/挑战 1.48，远征最低血 3/死亡 2 且连续推进至 `(-30,0)`，前哨 `(1,0)` 166 次结算后产生 1 条生态告警和 3 个裂口前线，缺当地容器时产出明确阻塞。回放行新增光压、挑战系数和区块坐标字段，未改变玩法。

```
__check()                  78 项 / 0 失败（主菜单 / 新局 / 回放后均验证）
__check({roundtrip:true})  78 项 / 0 失败
__srcCheck()               87 文件 / 0 失败
__watch(true,20)           1200 步 / 0 类违例
0 个未捕获 pageerror
```

### 4.3 难度曲线（`__replay`，种子 4242 · 守家型政策 · 3 天）

| 难度 | 死亡 | 最低血 | 3 级蚀痕 | 击杀 | 工人 | 封灯 |
|---|---|---|---|---|---|---|
| 安逸 calm | 1 | 4 | 0 | 42 | 3→2 | 第 2 天 |
| 标准 normal | **0** | **100** | 1973 | 36 | 2→2 | — |
| 严酷 harsh | 1 | 11 | 0 | 69 | 2→2 | 第 2 天 |
| **挂机对照 idle** | **3** | 4 | — | **0** | 2→**0** | — |

**判据：「无必死夜」与「无白给夜」必须同时成立** —— 挂机会全军覆没，正常打能活；这才说明难度有决策空间。

### 4.3.1 W18-G3 远征/前哨矩阵（`4242 / normal / 5 天`）

| 路径 | 死亡 | 最低血/末值 | 击杀 | 食物末值 | 燃料末值 | 蚀痕 | 光压/挑战 | 区块边界/前哨结果 |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| 守家 `home` | 1 | 2 | 156 | 77 | 0 | 2 | 18 / 1.48 | `(0,0)` |
| 有限远征 `expedition` | 0 | 100 | 96 | 41 | 336 | 2 | 0 / 1.00 | 只访问 `(1,0)`，第 1 天返营，`outOfRange=false` |
| 现场落箱驻守 | 0 | 末值 100 | 0 | 0 | 9 | 2 | 0 / 1.00 | 远端结算 164 次，箱内 `vine=43`，最终因当地食物不足暂停 |

> W17-F4 的“持续外漂至 `(-30,0)`”数据保留在 `docs/FIRST_SLICE.md` 作为历史压力样本；G3 的有限远征只约束开发回放，不限制正式游戏自由探索。

### 4.4 已知的、**故意不修**的事（别再当 bug 修一遍）

- 🧭 **地表只能撑约 3 天，之后必须下潜**：辉髓从第 2 天起恒为 0（矿脉有限，而它又是建材 + 燃料的唯一源头）。这是**内容节奏设计**，不是数值失衡（深渊矿更富 + 天然竖井已就绪）。
- 🧭 **蚀痕铺满是经济问题的下游**：已调过一档让它可救回；真解在上游产能，**不要靠继续调阈值或加难度乘子**去掩盖。
- ⚪ **炉子空烧火种**是设计（不是 bug）。
- ⚪ **难度乘子在第 8 步刻意一个没动** —— 加乘子只会掩盖真因。
- 📌 **完整的 7 天曲线**需要带「下潜」的 replay 政策才能测（现在的 `home` 政策不下潜）；塔升级/载荷经济学在曲线里也没被用上（辉髓总是稀缺）—— 属于已知盲区，**不阻塞**。

---

## 5. 可选方向（接手后从这里挑）

> 三条轴是**平行的**，**一次只做一条**（三轴并行会互相踩数值）。
> **顺序建议**：B 生存 → C 生态 → 其余收尾。

### B · 生存深挖（建议**先做**）

> 当前基线（第 9 步首轮回放）：5 天 `rest/expedition/panic` 三政策数据已写入 `docs/SURVIVAL.md`；后续调参仍以该表及 §4.3 为准。
> 第 3 步已验收：死亡会在原层留下可回收遗落包，玩家以 45% HP、35 饱食和擦伤状态回营；下一步进入第 4 步工事语言。

- **目标**：让「吃什么、怎么睡、死得起吗」真正成为决策
- **涉及**：`systems/blight.js` `systems/farm.js` `systems/mind.js` `systems/hazard.js` `systems/logistics.js`
- **已知缺口**：饥饿目前只有「不吃就掉血/掉士气」（第 8 步加了 25 秒缓冲 + 提示）；**温度、疾病、疲劳、睡眠、天气**这一整类压力都还没有；死亡代价目前是「回营地 + 燃料减半 + 跳黎明」
- **进度**：[`docs/SURVIVAL.md`](docs/SURVIVAL.md) 已于 2026-09-16 写成；第 0–7 步（含区块活跃/休眠与人工光刷怪纵切）已落地，S08 的 UI-1 背包已完成；界面后续拆分见 [`docs/UI.md`](docs/UI.md)。

### C · 生态深挖

- **目标**：世界本身会演化 —— 蚀痕长东西、野兽有作息、地形给资源也埋风险
- **涉及**：`world/gen.js` `systems/blight.js` `entities/enemy.js` `systems/relics.js` `systems/layer.js` `systems/hazard.js`
- **可复用的地基**：W14-A 第 3 步的**敌人行为框架**（`ABILITY` 表）就是为这条轴铺的
- **`DESIGN_V2.md` §7「生态反馈」还没实现**：蚀潮强度与你的光照总量挂钩
  `challengeMul = 1 + 0.06 × (总光压/基准光压 − 1)`，上限 +80% —— 即「你越亮，黑暗越想要你」
- **硬要求**：同 B，先写 `docs/ECOLOGY.md`

### D · 终局与结局（`DESIGN_V2.md` §8，未开工）

三座「共鸣信标」（每点亮一座触发**反冲大潮**守城战）→ 余辉之树共鸣 → **四种结局**（黎明 / 共生 / 孤独 / 献祭），结局画面用**本局真实数据**生成（存活名单、墓碑数、最大光压、夜采次数、蚀化者数）。
现存的大块空缺：**目前没有真正的"通关"** —— 第 7 天打完 Boss 只有一个序章横幅。

### G · 拓荒者轴（建议作为下一阶段）

将拓荒者从自动采集单位改造成可调度、可记忆、可救援的殖民地成员；玩家既是指挥官，也是成员。计划见 [`docs/COLONISTS.md`](docs/COLONISTS.md)：N0 数据/观测与迁移 → N1 任务板 → N2 调度界面 → N3 职业人格 → N4 救援 → N5 死亡/复苏 → N6 前哨协同 → N7 完整 E2E。

### H · 视觉与交互重制（与拓荒者轴并列，建议先做 V0–V1）

将原型式圆形单位、单色块地形和工具化面板重制为 Q 版像素人、可读蚀兽剪影、分层 HUD 与决策卡界面。计划见 [`docs/VISUAL_UI.md`](docs/VISUAL_UI.md)：V0 视觉地基 → V1 四向角色 → V2 世界美术 → V3 HUD → V4 世界交互 → V5 面板 → V6 无障碍 → V7 E2E。

### E · 光路下半：光谱分色（**作者主动押后**）

冷光（克蚀壳/盲兽 +20%，但夜枭无视）/ 暖光（农田 +40%、士气回复 +50%，但招噬光虫）。
押后原因：**"光有颜色"是又一个新维度**，要等前面几条轴消化完。

### F · 收尾项（非阻塞，随时可做）

| 项 | 归属 | 状态 |
|---|---|---|
| 12 张图 / 7 首音乐 / 30 条音效 / 3 张环境音 | **作者本人做**（内容） | 管线与接口**全部就绪**，交一张生效一张。清单：`assets/PROMPTS.md`（图）· `assets/SOUND.md`（声音） |
| 打包成 exe（Electron/Tauri） | 后续 | 作者明确"以后再做" |
| 真 z-level 与温度、事件总线 | 更远 | 仅在设计文档里提过 |

---

## 6. 注意事项（雷区）

### 6.1 高发 bug 家族（来自真实修理记录）

| 家族 | 长什么样 | 怎么防 |
|---|---|---|
| **① 跨模块隐式依赖** | 用了别的模块的导出却忘 import → `ReferenceError`（真炸过） | `__srcCheck()` 就是为它写的 |
| **② 返回值语义反直觉** | **多个核心函数以「返回 `null` = 成功、返回错误字符串 = 失败」**（`tryPlace` / `withdrawOne` / `sealError` / `__place`）→ 写成 `if (!fn())` 就是**判断反了** | 改这类代码前先读返回约定，注释里都写了 |
| **③ 多系统抢同一个输入** | 站在炉边按 E 被旁边的藤木抢走（交互优先级） | 改 `systems/interact.js` 的优先级后，务必真键鼠 E2E |
| **④ 派生数据不同步** | 缓存没失效 / 面板数字停住 / `state.res` 聚合值没更新 | 写完问一句"这个值的唯一来源是哪里" |
| **⑤ 表与实现分家**（B13） | 数据表改了、实现没跟着改（"表里写着能对空，代码里没判"） | 每张表都要有**交叉校验断言**；新加的机制**必须同时加断言**，否则下一轮又漏 |
| **⑥ 断言空跑报绿**（D48/D51/D56，**复发 3 次**） | 断言只在某个世界状态下才被执行 → 三种局面全绿，第四种局面才爆 | 见 §3.2 两条硬规矩 |
| **⑦ 帧内成对的"置位-复位"标志**（D55） | 死亡标志在同一帧内被置位又被清掉 → 帧边界采样永远看不到 | 采样要放在正确位置，或改用可观测的派生量（如"血量骤增 = 刚复活"） |

### 6.2 硬约束（**不要做的事**）

1. **不引入任何依赖**：不加 npm / 打包器 / 框架 / TypeScript / 测试框架。这是一个"双击就能跑"的零构建项目，也是它能被 AI 反复改动的前提。
2. **设计铁律**（`DESIGN_V2.md` §1）：
   - ❌ **不做经验值 / 等级** —— 变强必须来自"你理解了世界"，不是"打了 300 只怪"
   - ❌ **不做装备词条池** —— 不要"+12% 暴击"这种数值堆积；所有升级都是**行为/规则层面**的
   - ❌ **不做无限刷怪与资源通胀** —— 难度来自**新行为 + 组合 + 信息**，不是更多更硬的怪
   - ✅ **唯一准入标准**：任何新机制必须回答「**它改变了玩家今晚/这一局做什么决策？**」答不出来就砍掉
3. **不破坏三个核心**：
   - **光照 = 生命线**（任何新敌人/技能都不能让"光"失去意义）
   - **账本守恒**（加/减资源必须成对，走 `deposit`/`withdraw`，不直接改 `state.res`）
   - **帧预算 16.6ms**（峰值夜 `frame.total` 现在约 1.67ms，宽容到 8ms；每步都复测 `__perf()`）

### 6.3 与作者协作的方式（**很重要**）

- 作者**用中文**，指令极短（常常就是一句「下一步。」），**期待你自主执行**，但要求：
  1. **一步一验**：实现 → 真浏览器/真键鼠验证 → 同步文档 → 报告
  2. **诚实**：自己的失误也要写进台账（项目里有 **D1–D56** 一整列"检测器自己犯的错"，那是刻意的）
  3. **给数字，不给感觉**："感觉好多了"不算完成；要给 `__check` 项数、曲线读数、逐项对账
  4. 报告格式大致是：**做了什么 / 判据与数字 / 我自己的错误 / 必须告知你的事 / 下一步**
- 破坏性操作（删档、改存档格式、改数值表）**先说再做**。

---

## 7. 文档地图（哪个是活的、哪个是历史）

| 文档 | 性质 | 什么时候读 |
|---|---|---|
| **`HANDOFF.md`（本文）** | **活 · 入口** | 任何时候 |
| **`AGENTS.md`** | **活 · Codex 自动加载** | 每次会话自动读 |
| `GUIDE.md` | **活 · 玩家手册** | 想知道"玩家实际看到什么"时（世界观/四层/资源/操作/研究树/上手建议） |
| `README.md` | 活 · 门面 | 简介与跑法 |
| `DESIGN_V2.md` | **活 · 设计蓝图** | **要做新机制前必读**（含 7 条主轴 D1–D8、设计铁律、四条结局）。注意 §9 路线图是历史进度记录 |
| `docs/COMBAT.md` | **活 · 纪律范本 + W14-A 台账** | 开工新轴前**照抄它的格式**（判据先写死 → 分步 → 每步可验收 → 台账）。战斗轴本身已 ✅ 收口 |
| `docs/SURVIVAL.md` | **活 · W15-B 生存轴计划** | 第 0–7 步已验收；第 9 步首轮回放基线已记录 |
| `docs/UI.md` | **活 · 界面优化计划** | UI-1～UI-4 已完成；第 9 步回放基线已记录 |
| `docs/ECOLOGY.md` | **活 · W15-C 生态与大地图计划** | E0–E5 首版已完成；后续为生态轴全量回归与内容打磨 |
| `docs/COLONISTS.md` | **活 · W16-D 拓荒者轴计划** | N0–N7 已验收：身份卡、`__crew()`、任务板投影、调度面板、工作区/前哨/夜班/救援指令、职业/性格行为节点、倒地窗口、现场救援、铺位护送、恢复期、医疗站、有限治疗队列、行医职业、照护意图调度、死亡履历、关系记忆、单次余烬复苏、计时跨区迁移、可中断撤回、低频远端采掘、本地容器入库、远端补给、光压/蚀潮前线记账、有限求援告警、进入区块后告警兑现、地图身份标记、个人经历记录、招募→远征→救援/死亡→关系→复苏→存读与三策略十日回放；后续扩展另立计划 |
| `docs/VISUAL_UI.md` | **已收口 · W16-E 视觉与交互重制计划** | V0–V7 已落地；五日回放/性能/四态自检通过，登记素材 17/17 已接入，面板入口已原生按钮化；远征回放、跨区块库存、Canvas 放置/采集/点火/容器/安抚右键链与长跑监控均已实测 |
| `docs/FIRST_SLICE.md` | **活 · W17-F 首局垂直切片计划** | F0/F1/F2/F3/F4 已完成：阶段契约、存档/读档归一化、`__slice()`、回放隔离、真实采集/点灯软引导、黄昏/蚀潮/余波观测、一次真实区块往返、三路径固定矩阵；后续调整另立小步 |
| `docs/EXPEDITION_OUTPOST.md` | **已收口 · W18-G 远征与前哨补给闭环** | G0/G1/G2/G3 已完成；后续另立内容打磨小步 |
| `docs/BUG_HUNT.md` | **活 · Bug 台账** | 改代码前扫一眼高发家族；**新 bug 要按格式登记**（含检测器自身错误列 D1–D81） |
| `docs/COPY.md` | **活 · 文案规范** | **改任何玩家可见文字前必读**（三原则 + 禁用清单 + 术语表 + 全量文案） |
| `docs/PAYLOAD.md` | 历史 · W14-A 第 2 步子计划 ✅ | 要动载荷系统时查设计取舍 |
| `OPTIMIZE_PLAN.md` | **历史 · W13 大工程台账**（651 行，全部已落地） | **不必通读**；只查 §0 的"剩余项"表。要动 UI/性能/音频前可查对应章节的"实际落地与差异" |
| `assets/PROMPTS.md` `assets/SOUND.md` `assets/README.md` `assets/*/README.md` | 活 · 素材清单与管线 | 交素材 / 加素材时 |

> **文档纪律**：每完成一步，**同步更新** ①对应计划文件的 ▶ 标记与数字 ②`docs/BUG_HUNT.md` 台账 ③本文 §4 的进度与基线 ④`README.md`（如果玩家可见的东西变了）。
> 文档与代码不一致时，**以代码为准**，但要立刻把文档改对 —— 下一任接手者只会读文档。

---

## 8. 接手后的前 30 分钟（推荐动作清单）

1. 跑起来：`Start-Process py -ArgumentList "-3.12","serve.py","8000" -WindowStyle Hidden -PassThru` → 打开 <http://localhost:8000>
2. 控制台跑基线：`__check()` / `__check({roundtrip:true})` / `__srcCheck()` → 确认 **78 / 78 / 87，0 失败**；再看 `__slice()` 是否为 `wake`
3. `__newGame(4242,'normal')` → `__steps(60*400)` 走一天，看 `__testLog()` 有没有违例；`__combat()` `__wave()` `__dps()` 各看一眼（**这是你的仪表盘**）
4. 跑一次难度曲线：`__replay({ seed: 4242, diff: 'normal', days: 3, policy: 'home' })` → 跟 §4.3 的表对账（**对得上，说明环境正常，可以开工**）
5. 真玩一局（WASD + 鼠标，按 `P` 暂停思考），体会昼夜节奏与"光 = 生命"
6. 读 `DESIGN_V2.md` §1 设计铁律 + §5 可选方向对应章节
7. **开工下一步**：阅读 [`docs/EXPEDITION_OUTPOST.md`](docs/EXPEDITION_OUTPOST.md) 的 G1 验收记录；下一步做 G2 现场建箱与补给闭环，不混入无限刷怪或离线产出

---

## 附：一句话记住这个项目

> **光给你一切，也召唤一切。**
> 所有机制都挂在"光 = 燃料 = 生命 = 安全区"这一条链上；所有工程纪律都挂在"机器找 bug、数字说话"这一条原则上。
> 破坏这两条中的任何一条，这个项目就散了。
> N7 文档更正（2026-09-18）：拓荒者计划 N0–N7 已完成；个人经历、区块成员引用、十日三策略回放、复苏与性能收口均已记录在 `docs/COLONISTS.md`，后续扩展需另立计划。
