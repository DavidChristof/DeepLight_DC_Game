# 《蚀渊拓荒者 Deep-Light》优化方案 · W13 阶段大工程

> **状态：W13-A ~ W13-N 全部已落地 ✅ · 本文是历史台账，不必通读。**
> 当前进度、验证纪律与下一步以 [`HANDOFF.md`](HANDOFF.md) 为准。只在两种情况下回来查：
> ① §0 的"剩余项"表；② 要动 UI / 性能 / 音频 / 素材时，查对应章节的"实际落地与差异"。

> 作者：Copilot（以本作主程 + 主策身份撰写） · 对象：David · 2026-09-11
> 参考坐标：**Minecraft**（建造手感 / 快捷栏）、**泰拉瑞亚**（鼠标职责 / 自动选择工具）、**环世界**（信息层级 / 暂停决策 / 区域批量）
> 硬约束：保持**零构建、零依赖、纯原生 ES Modules**；守住铁律「不做等级 / 词条 / 无限刷怪」；不引入框架与打包器
> 本文只讲**改什么、为什么、怎么验收**，不动设计主轴（光 = 输出、人 = 代价）

---

## 0. 一页结论：优先级与批次

| 批次 | 主题 | 玩家能感觉到什么 | 工作量 | 风险 | 依赖 | 状态 |
|---|---|---|---|---|---|---|
| **W13-A** | **操作手感** | 「终于能像正常游戏那样操作了」 | 中 | 中（会改核心输入） | 无 | ✅ 已落地 |
| **W13-B** | **文案与信息架构** | 「一眼就懂，不再像看说明书」 | 中 | 低（纯文本/UI） | 无 | ✅ 已落地 |
| **W13-C** | **性能与算法** | 「低配机也能 60 帧；不卡顿不掉帧」 | 大 | 中（碰渲染/光照） | 先做 4.1 测量 | ✅ 已落地（见 §4.11） |
| **W13-D** | **音效系统** | 「有反应了」——点下去有声音 | 中 | 低 | 无（纯合成，零素材） | ✅ 已落地 |
| **W13-E** | **视觉与图标** | 「看上去像个完整的游戏」 | 中 | 低 | 可先做图标 | ✅ 已落地（见 §3.9） |
| **W13-F** | **图片素材接入** | 「有脸了」 | 大 | 中（需素材来源） | 需你先定素材来源 | ✅ 管线已通（见 §5.7） |
| **W13-G** | **节奏拉长 + 黎明消解**（计划外 · 用户点名） | 「白天终于是主场了；天亮不是啪一下全没」 | 小 | 低（只动时间常量与波次） | 无 | ✅ 已落地（见 §4.12） |
| **W13-H** | **声音文件通道 + 交付清单**（计划外 · 用户会编曲） | 「我能把自己的音频丢进去」 | 中 | 低（缺文件全部降级） | 无 | ✅ 已落地（见 §5.8） |
| **W13-I** | **操作三修 + 首次提示**（计划里“本批未做”的那几条） | 「右键不再偷偷放东西；快捷栏要的时候才出来；回车就能确认」 | 中 | 低 | 无 | ✅ 已落地（见 §2.7 / §3.10） |

**剩余项（全部非阻塞 —— 计划主体已收尾）**

| 剩什么 | 归属 | 说明 |
|---|---|---|
| 12 张图 / 7 首音乐 / 30 条音效 / 3 张环境音 | **你**（内容） | 管线与接口全部就绪，交一张生效一张。清单：`assets/PROMPTS.md`（图）· `assets/SOUND.md`（声音） |
| ~~容器面板拖拽搬运（§3.5）~~ ✅ W13-J 已做 · ~~面板内搜索（§3.4）~~ ✅ W13-L 已做 | 可选 | 这两件都落地了；剩下的可选小件主要是更多素材/内容 |
| 打包成 exe（Electron/Tauri） | 后续 | 用户已确认“放以后计划做”，不属 W13 |
| 第三阶「熔渊之心」 + 灯塔结局 | 后续版本 | W13 只做优化与打磨，不加层数/终局 |

> 已完成的“计划里标了未做”的项：`Shift` 快走、拖矩形填充、面板内 `Enter` 确认、首次提示 `state.seen` —— 均在 **W13-I**（见 §2.7 / §3.10）。

**推荐顺序：B ✅ → A ✅ → D → C → E → F**
理由：**B（文案/信息）改动最小、玩家感知最强**，且它是 A/E 的地基（组件与文案规范先立起来）；**A（手感）** 是"反人类"的重灾区，越早改越省事；**D（音效）** 性价比极高（零素材、一天量级）；**C（性能）** 放在功能稳定后做，避免边改边优化白做工。

> 拍板记录（问卷未答上来 → 按推荐默认执行）：① 顺序 A → D → C → E → F；② 快捷栏全局 + 右键上下文都要；③ 音效全合成；④ 素材先搭管线（世界层保持程序绘制）；⑤ 性能下限 = **2 核 2G 云主机 + 集显**。

---

## 1. 现状体检（有据可查）

### 1.1 体量

| 项 | 数值 |
|---|---|
| 代码总量 | ≈ 8,600 行（`main.js` 1035 · `render.js` 928 · `panels.js` 770 · `worker.js` 650 · `style.css` 580） |
| 地图 | 地表 96×72 = **6,912 格**；视野 45×30 = **1,350 格** |
| 模拟 | 固定步长 1/60；`MAX_ACC 0.25` → **单帧最多补算 15 步** |
| 素材 | **0 张图片、0 个音频**（全部是 Canvas 程序绘制） |

### 1.2 性能热点（每条都有代码证据）

| # | 位置 | 现状 | 为什么是问题 |
|---|---|---|---|
| 1 | `world/light.js` `compute()` | **每个 simStep 都把整张光图重算**，`new Float32Array(6912)` 每次分配 | 正常 60 次/秒；卡顿补帧时 **最多 300 次/秒**，即 ~8 MB/s 垃圾 + 数十万次 BFS 节点访问 |
| 2 | `light.js` `spread()` | 内层循环每访问一格就构造 `[[x+1,y],[x-1,y],[x,y+1],[x,y-1]]`（**4 个数组/格**） | 单盏灯半径 7 ≈ 154 格 → 每次 600+ 数组；40 盏灯 + 岩浆 + 墓碑时是**每帧上万次分配** |
| 3 | `systems/render.js` 主循环 | 每格 `mix()`（**返回新数组**）+ `speck()`（每次 `Math.sin` 取模）+ 拼接 `rgb(...)` 字符串 + `fillRect` | 1,350 格 × （2~7 次 mix/speck + 1 次字符串）→ **每帧上千次分配**，GC 抖动、低端机掉帧 |
| 4 | `world/pathfinding.js` `findPath()` | 每次调用 `new Int32Array(w*h)`（**22 KB**）+ `dirs` 数组 + `smoothPath` 里 `lineClear` 用 `Math.hypot` 0.2 格采样 | 每个拓荒者目标重置就重算一次；多工人时同一帧可分配几百 KB |
| 5 | `systems/storage.js` `syncRes()` | 每次 `deposit/withdraw` 全量遍历所有容器、**新建对象并替换 `state.res`** | 采集/炼油每秒 6~20 次写入 → 每次一堆临时对象 + 排序（`deposit` 里 `cs.sort`） |
| 6 | `systems/minimap.js` | **每帧**重写 6,912 像素的 `ImageData` + `putImageData` | 地图只在挖掘/探明/蚀痕变化时变，其余 99% 是纯浪费 |
| 7 | `main.js` `simStep()` | `state.floaties.filter(...)` 每步**新建数组**；多处 `Math.hypot`（比 `sqrt` 慢 2~4 倍） | 每帧零分配目标被打破 |
| 8 | DOM | `renderPanelHost` / `renderSidebar` 已做**两层刷新**（结构签名 + `data-live`）✅ | 这块做得好，保留；但 9 个 tab 会放大重建成本 |

### 1.3 操作现状（* = 反人类）

| 输入 | 现在干什么 | 问题 |
|---|---|---|
| 左键 | 点一下 → 寻路走过去 | **按住不会跟手**（泰拉瑞亚/暗黑都是按住持续走）；也没有"按住连续采集" |
| **WASD / 方向键** | **已经能直控移动**（`input.js` 的 `MAP` + `moveManual`） | ***帮助行完全没写**，玩家不知道 —— 典型"功能有、引导无" |
| E | 按住重复交互 | 长按/单击没有区分，误触与"按了没反应"（无反馈音）都靠飘字救 |
| 右键 | 仅"取消建造" | 全游戏**最贵的一个键被浪费**（MC/泰拉：放置/使用/自动选择） |
| 滚轮 | 无监听 | 缩放、切换方块、快捷栏切换全都缺失 |
| 数字键 | **只在建造面板开着时**选类内第 N 项 | 与 MC/泰拉"快捷栏"肌肉记忆冲突；关面板后数字键全废 |
| X | 切换拆除模式 → 再左键 | 两步操作；**Alt+左键**（泰拉/环世界的"直接拆"）更顺手 |
| 建造流程 | 按 B → 选类 → 选块 → 关面板 → 左键放 | **4~5 步**，且**一次只能放一个**（建 10 面墙要点 20 次） |
| 滚轮/中键 | 无 | 没有"中键拖屏"（环世界）与"取色/取样"（MC 中键复制方块） |
| 相机 | 只能跟随玩家 | 无法平移查看营地全貌（建大工程时很痛） |

### 1.4 UI / 文案现状（"AI 味"标本）

| 位置 | 现在的文案 | 病症 |
|---|---|---|
| `index.html` `#help` | `左键移动 · E 交互/施工 · 空格 光爆 · B 建造（面板分类选块） · N 夜行 · T 研究 · C 图鉴 · R 灯光 · P 暂停 · Esc 菜单` | 8 项挤一行 + 括号解释 + **漏了 WASD** |
| 建造提示 | `建造中：灯柱（辉髓2 藤木2· 工期 8 工） · 左键放置工地 · Esc 取消` | 「建造中：」+「左键放置工地」是废话（正在建造时当然） |
| 熔炉面板 | `研究「自动熔炉」后可建无人值守的那一台 —— 同一套配方，代价是得持续喂火种` | 破折号长解释、复述规则、没有动作 |
| 手做提示 | `按住 E 连续做（每 0.50s 一批）· 火烧得越旺越快，但要你自己站在这里` | 括号注释 + 「但要你自己站在这里」是情绪句 |
| 火种尾注 | `火种不是燃料专属 —— 藤木能凑合、木炭最划算、燃料火力最猛（烧的是光的血）` | 三段解释，玩家只需要"哪个更划算" |
| 冷炉提示 | `⚠ 炉子冷着：走到炉边按一下 E 就是点火（藤木/木炭/燃料都能烧）` | 说教式；需要的是「按 E 点火」 |
| 库存行 | `辉髓 42（扣的是容器里的料，成品也进容器）` | 括号里是**系统实现细节**，不该给玩家看 |
| 术语 | 同一资源在 GUIDE/README/飘字里写作 **`辉髓`** 与 **`辉髄`** 两种（`worker.js:370`、`interact.js:146`、GUIDE 建造表） | 术语不统一 = 不专业 |
| 存储面板 | 左栏右栏各列 **`STORE_ORDER`（10 材料 + 4 工具 = 14 行）**，没东西也占一行 | **用户点名**：应"箱子里有什么就列什么" |
| 面板 tab | `建造 夜行 研究 图鉴 容器 制造台 熔炉 操作台 解析台`（9 个） | 4 个是"按需浮出"的站台面板，不该常驻 tab 条 |
| 侧栏 | 材料 7 行 + 分隔线 + 营地 4 行 + 装备 + 拓荒队名册 | 信息密度偏高，缺少"分组/折叠/图标" |

### 1.5 音频 / 素材

- **音频**：全项目 `grep AudioContext|sfx|audio` = **0 命中**。GDD 里已规划"WebAudio 合成音效（零素材）"但未实现。
- **素材**：`assets/` 不存在，无 `Image` 加载，无 manifest；所有图形都是 `fillRect` 程序绘制（这是优势：零加载、可无限缩放；也是天花板：表现力受限）。

---

## 2. 操作手感（W13-A）

> 目标一句话：**"想走就走，想建就建，想拆就拆，不用先学一套新手势。"**
>
> **✅ 本批已落地（见 §2.6 差异说明）**：按住跟随 / 拖放连放 / Shift 补齐 / 右键上下文 / Alt 扫拆 / Ctrl+Z 全额退料撤销 / 滚轮（切格 | 缩放）/ 中键拖屏 / 快捷栏 + Q / Tab 轮换 / 误触保护 / toast 反馈。

### 2.1 鼠标职责重划分（**核心改动**）

| 操作 | 现在 | 改为 | 对标 |
|---|---|---|---|
| 左键单击 | 走过去 | **走过去**（保留） | 环世界 |
| **左键按住** | 无 | **持续朝光标走**（松开停下）；建造模式下 = **连续放置**；采集目标上 = **连续 E** | 泰拉瑞亚 / 暗黑 |
| **右键** | 仅取消 | 上下文动作：**建造模式 = 放下 / 旋转** ；对准建筑 = **加油 / 开工**；对准可采物 = **采集**；空处 = 取消 | 泰拉瑞亚（右键 = 使用） |
| **滚轮** | 无 | 建造模式 = **切换方块**；普通模式 = **缩放**（0.75×/1×/1.5×/2×） | MC |
| **中键** | 无 | **拖拽平移相机**；对准建筑 = 取样该方块进入建造模式 | 环世界 / MC |
| 右键拖动 | 无 | 建造模式下**拖出直线**（Shift 锁正交）：一次放一排墙 | MC（拖放） |
| **Alt + 左键** | 无 | **直接拆除**（不必先按 X）；拆除时显示返还材料数 | 泰拉 |

### 2.2 键盘

| 键 | 现在 | 改为 |
|---|---|---|
| WASD / 方向键 | 可用但没写 | **写进帮助**；`Shift` 按住 = 快走（×1.5，不消耗额外资源，只在白天有效） |
| 1–9 / 0 / − | 仅面板开启时 | **快捷栏**（9 格）：面板开着 → 选类内第 N 项（保留既有习惯）；关着 → 直接进入建造模式并选中该格 |
| `Q` | 无 | **切换上一格快捷栏**（滚轮同义） |
| `Tab` | 无 | 循环切换面板（建造→研究→图鉴→夜行→关闭）—— 全键盘可达 |
| `Enter` | 无 | 确认当前面板中被高亮/悬停的条目（键盘流） |
| `Ctrl+Z` | 无 | **撤销上次放置/拆除**（30 秒内有效，退回材料） |
| `X` | 切拆除模式 | 保留（长按更顺？先保留单击切换） |
| `Esc` | 分层取消 | **语义分层表**写进设置页：建造中 → 取消建造；面板开 → 关面板；否则 → 暂停菜单 |

> **快捷栏设计**：底部 `#panelbar` 上方新增一排 9 格图标条（`#hotbar`），显示图标 + 名称 + 材料是否足够（不足 = 红字）；建造模式下高亮当前格。**它同时解决"9 个 tab 太挤"** —— 站台面板（容器/制造台/熔炉/操作台/解析台）从 tab 条移除，改为"靠近时在屏幕下方浮出一行提示"（已有 `E：打开熔炉` 提示，只需加一个可点的按钮）。

### 2.3 建造流：从 5 步到 2 步

```
现在：B → 点分类 → 点方块 → 关面板 → 左键
改为：1..9（或滚轮+Q）→ 按住左键拖出形状
```

- **放置预览增强**：幽灵格上叠加**作用范围**（灯 = 光照圆、塔 = 射程圈、农田 = 光照需求提示、竖井 = 层提示）—— 这是"信息即决策"，也是 MC/环世界都有的。
- **直线/矩形**：Shift + 拖 = 直线；Ctrl + 拖 = 矩形填充（墙与地板一次性铺）。
- **连续放置间隔**：默认 0.12s/格，与网格吸附；避免手抖放歪。
- **放置/拆除的即时反馈**：音效（W13-D）+ 一次性飘字（不再每次都给长句）。

### 2.4 交互一致性与"不误伤"
- **交互优先级**已有 `resolveInteract` 顺序（工地 > 节点 > 站台 > 加油 > 采收 > 竖井 > 容器）→ 保留，但新增**可视化**：地面高亮圈 + 目标名（已有 E 标签）+ **按住时显示进度弧**（已有）。
- **危险动作二次确认**：拆除**已建成**建筑若价值 ≥ 5 材料，要求 **Alt 双击** 或提示"再按一次"（1.5 秒窗口）。工地（全额退料）不确认。
- **误触保护**：面板按钮点击后 0.2s 内忽略重复点击（现在连点会连扣材料，比如「加满」「制作」）。
- **长按 vs 单击**：E 统一改为「**单击 = 做一次**；**按住 = 连续做**」，与现在行为一致但**在帮助里写清**，且失败时给红色短提示（已有飘字）。

### 2.5 明确不做（避免范围爆炸）

- ❌ 跑动/冲刺的体力机制（会引入新资源轴）
- ❌ 手柄支持（本轮不考虑）
- ❌ 触屏/移动端布局（另开一期）
- ❌ 自定义按键绑定（先固定，等键盘表稳定再说）

### 2.6 实际落地与初稿的差异（已实现）

| 初稿 | 实际做法 | 为什么 |
|---|---|---|
| 中键对准建筑 = **取样** | 中键只做拖屏 | 取样有“按错就进入建造模式”的副作用；快捷栏 + Q 已经够快 |
| **Alt 双击**确认拆已建成建筑 | **去掉二次确认**，Alt 本身即“我很确定”；拆完给 toast + 下次 Ctrl+Z 有窗口 | 确认弹窗正是“反人类操作”；误拆补救改成“可撤销 + 可重放（5 折退料）” |
| `Shift` 按住 = 快走 | ✅ **W13-I 已做**（见 §2.7）：5 → 7.75 格/秒。担心过的"与寻路/避怪节奏耦合"没有出现 —— 它只是倍率，`followPath` 与 `moveManual` 共用 | 快走不消耗资源、不改光照与恐惧节奏；觉得太快/太慢改一个常量 `RUN_MUL` |
| `Ctrl` + 拖 = **矩形填充** | ✅ **W13-I 已做，而且不用 Ctrl**：建造模式下**直接按住左键拖 = 矩形**（用户点名）。理由：Ctrl 会与 Ctrl+Z 撤销、Ctrl+点击等混在一起，而"按住左键"本来就是这个游戏唯一的拖放手势 | 松手才落地（拖的时候画虚线范围 + 格数），`Alt+左键` 的"直接拆"不受影响；上限 1600 格（40×40） |
| 右键拖动 = 直线 | **按住左键拖 = 自由绘制/直线补齐**；右键只管“对光标处做事” | 左右键职责不要重叠（MC 里右键=使用）；拖放全交给左键一条路径 |
| `Enter` 确认面板高亮项 | ✅ **W13-I 已做，而且是"鼠标指着哪行就是哪行"**：不引入键盘焦点系统，因为方向键还要当移动保底，被面板抢走就会在面板里乱跳 | 焦点圈是**描边**（`.foc`）而不是改颜色 → 不动任何文字对比度，也不与"可选/不可选"的配色打架 |
| 交互优先级（工地 > 节点 > 站台 > 加油 > 采收 > 竖井 > 容器） | ✅ **W13-K 修掉了一个真 bug**：E 的优先级改成 **工地 > 安抚 > 站台 > 节点 > 夜辉草 > 加油 > 农田 > 竖井 > 容器**。原来站台排在采集节点**后面**，而炉子多半就建在矿脉/藤木旁边 → 按 E 变成砍树、玩家以为“点不着火”（实测复现：同一位置，旁边硬塞一格藤木，`__resolve()` 就从 `station/furnace` 变成 `mine/vine`）。多台机器挨着时取**最近**的那台（不再拿数组里第一台）<br>右键不受影响：`resolveInteractAt` 本来就按格子解析，站台依旧最优先 |
| 右键 = 对光标处做事 | ✅ W13-A/K：右键按**格子**解析，优先级：站台（含冷炉子点火）→ 熟农田 → 竖井 → 灯类加油 → 节点 → 夜辉草 → 容器；**有 `fireMat` 的炉子不给加燃料** | 右键是“对那一格做事”，不能拿身边的优先级去覆盖它；炉子烧的是火种，加燃料是错的 |
| 站台面板从 tab 条移除 | ✅ **W13-I 已做**：面板定义标 `tab:false`，标签条只留 `B 建造 · Q 快捷建造 · N 夜行 · T 研究 · C 图鉴`；站台照旧靠**走近按 E** 打开（实测熔炉面板无标签但能开） | 功能方块只会越来越多，标签条一行放不下 |
| **右键在建造中会放蓝图**（实现 bug） | ✅ **W13-I 修掉**：右键 = **退出建造**。原本是"左右键都能放"，与"右键=对光标处做事"的设定直接冲突，玩家会以为右键是取消/退出 | 一个键一个职责：左键放、右键退 |

### 2.8 熔炉工作 / 自动存取修正（W13-K，用户报的 bug）

用户原话：「熔炉的工作/自动存取有问题，有时候烧不起来，有时候不会自动补燃料。」查下去是 **三个独立的毛病**，逐条实测复现后再改：

| 毛病 | 根因 | 改法（实测结果） |
|---|---|---|
| **“有时候烧不起来”** | **E 的交互优先级把站台排在采集节点后面**（见 §2.7 那行）。炉子建在矿脉/藤木旁边时，按 E 会去砍树 → 玩家只能一次次换位置碰运气 | 站台提到节点之前（安抚仍在它前）。实测：同一位置同一炉子，旁边硬塞一格藤木，`__resolve()` = `station/furnace`；走开 2.6 格后那一格藤木仍能按 E 采 |
| **“不会自动补燃料”** | 工人的添火任务用**阈值即撤**：`fuel > 34%` 就不管了 → 添 1 个就撒手走人，槽永远半空（实测 4→5 反复），夜袭/蚀潮一打断就熄火 | 改成**接了手就填满**（阈值只决定“要不要去”），添火节奏 1.1s→0.7s；容量满时解绑（不然会常驻）；**尊重熄火开关**（以前 addFire 会顺手把 `off` 清掉，玩家按的“熄火”被工人偷偷推翻）；工人不在时不去碰手炉 |
| **“自动存取有问题”** | 本层容器满时，机器**照旧扣料、成品当场丢**（有报警但玩家只看到“炉子不出货”） | 出货前先看有没有地方放（新增 `hasRoomFor`）：**放不下就不扣料**，并把 `dry` 标成 `no-room`（面板显示“容器满・成品没处放”）；腾出空间后自己恢复 |

还顺手修了两处小的：① 工人点火给手炉只放 1 个火种（藤木 7s 就灭，手做 3.5s/批 → 每 7 秒闪一次），现在点火后顺手补到 4 个；② 自动熔炉的“状态”行早先会把“没火”显示成“缺料・辉髓”（`dry` 是旧值），现在四态分清：停工 / 没火种 / 缺料 / 容器满，且进增量刷新（不用重建面板）。

> 实测（真键鼠 + 3 个游戏日）：火种**一次都没掉到 0**（修前会掉）、产出 216 批、工人只有 1/20 采样被绑在炉边；熄火后 60s 无人来点；容器塞满 50s 内 **ore 0 / made 0**（不再白烧），腾 5 格后自己接着出 5 批；按住 E 1.6s = 面板打开 + 点火 + 一手批（旁边那格藤木没被砍）；右键（`resolveInteractAt` 那条路）冷炉子 → 点火扣 1 藤木，再点 → 手做 1 批（-1 辉髓 +1 燃料）；零报错。

> 已验证（真实鼠标/键盘）：按住跟随 6.5 格 · 拖放 3 面墙 · Shift 补缝（跳跃格被标注原因）· Alt 扫拆 · Ctrl+Z 全额退料 · 右键加油/点火开面板/采集/取消 · 右键远处目标 → 自己走过去 4.1 格 · 中键拖屏后走一步自动收回 · 滚轮 fit→1× · Tab 轮换 · 快捷栏渲染（未解锁变暗/缺料标红）· **3 个游戏日回归零报错**。

### 2.7 操作三修（W13-I）：右键职责、快捷建造栏、拖矩形

> 这一批是用户点名的：一条实现 bug + 两条"计划里标了未做"的项 + 一次 UI 瘦身。验收全是真键鼠。

| 项 | 落地 | 关键决定 |
|---|---|---|
| **右键 = 退出建造** | `contextmenu` 的建造分支从"放蓝图"改成"退出建造"（清 `building/hotSlot/dragRect` + toast + `click` 音） | 建造中曾经**左右键都能放**（当时的理由是"不用换手"），但那与"右键=对光标处做事 / 空处取消"直接冲突。一个键一个职责 |
| **快捷建造栏按 Q 开关** | `KeyQ` 从"换分类"改成"开关快捷建造栏"；换类改用 `[` `]`（两个独立动作，可改键）；数字键在**没开栏时顺手把它叫出来** | 收起时**顺手放下手里的蓝图**："栏关了就是没在建"。它**只是一个开关，不开右侧页面**（那页和 B 建造完全重复，用户点名不要）—— 底部标签上的 `Q 快捷建造 ▲/▼` 是同一个开关，`Tab` 轮换不包含它 |
| **站台面板不占标签位** | `PANELS` 里给 5 个站台面板加 `tab:false`；标签渲染与 `TAB_PANEL_IDS` 都按它过滤 | 只改"要不要出现在那条上"，不动面板定义 —— 走近按 E 的路径完全不变 |
| **拖矩形填充** | `mouseDuties` 里按住左键只**记录矩形**（`state.dragRect`），`window.mouseup` 才 `placeRect()` 一次性落地；渲染里加 `drawDragRect()`（虚线框 + `矩形 6×4 = 24 格 · 松手放下`） | ① 单击 = 1×1 的矩形，**同一套逻辑**，不会两种放法打架；② 快到来不及画框的一下，用 `_dragStart` 兜底；③ 上限 1600 格（40×40）并变红拒绝；④ 落地失败按**一条汇总提示**报（`放下 13 格 · 3 格没放下（太远）`），不能刷屏；⑤ 暂停/菜单里松手不算数 |
| **Shift 快走** | `Colonist.speedMul` + 每帧从 `ptr.shift` 写入（`RUN_MUL = 1.55`） | 它只是倍率：不消耗资源、不改光照与恐惧节奏，`followPath`（鼠标跟随）与 `moveManual`（键盘）共用 |
| **面板内 Enter 确认** | `panels.js` 导出 `panelHoverAt / panelConfirm`；焦点存成 **`type:lamp` 这样的 key**（不是 DOM 引用），重建后按 key 找回来 | 焦点来源是**悬停**而不是方向键：方向键要当移动保底，被面板抢走就会在面板里乱跳。回车 = `dispatchEvent(click)`，所有既有处理逻辑原样复用 |

**踩坑**

1. **"回车确认"不要自己造第二套激活逻辑**：让 Enter 直接给目标行发一个 click 事件，面板里原有的选中/解锁/选配方逻辑一行都不用改，也不会出现"鼠标点有效、回车无效"的分裂。
2. **焦点不能存 DOM 元素**：面板每帧可能重建（结构签名变了就 `innerHTML` 重建），存元素引用第二帧就失效。存 `data-*` 的 **key**，重建后重新查。
3. **拖拽类交互必须"松手才落地"**：一开始想在拖动过程中逐格放（旧行为），但那样"拖回头"不会撤回，而且预览和结果不一致。改成"拖时只画框、松手才提交"后，玩家能反悔。
4. **上限要给**：一次 6912 格的矩形会让单帧明显卡一下（每格都要过一次 `placeError`），所以封 1600 格并给红色提示。
5. **删掉的功能要连文案一起删**：`Shift+拖 = 直线补齐` 让位给快走后，`lineTiles()`、信息页的两处说明、键位表 hint 都得同步，否则会留一堆"文档里有、游戏里没有"的假功能。
6. **“开关”和“面板”不是一类东西**：快捷建造栏最初被做成了正式面板（右侧一页列全本类），用户一看就说"那和 B 建造有什么区别"。修正：**一个开关只改一个可见物**（下面那一条），别顺手再开一页 —— 否则只是把重复的信息换了个地方摆。

---

## 3. UI、信息架构与文案（W13-B）

### 3.1 文案三原则 + 禁用清单

**三原则**
1. **一行说完**：任何提示 ≤ 24 字；超了就拆到 tooltip。
2. **说"做什么"，不说"为什么"**：为什么放 tooltip / 图鉴 / 载入提示。命令式动词开头。
3. **用玩家已经知道的词**：术语表统一（见 3.3），不用"系统内部词"（容器/聚合/派生/实例）。

**禁用清单**（写进 `docs/COPY.md`，改文案时照着删）
- ❌ 破折号长解释 `—— …`
- ❌ 括号补充说明 `（扣的是容器里的料，成品也进容器）`
- ❌ 复述按钮/状态 `建造中：…左键放置工地`、`运行中：发光 + 每 4 秒做一批`
- ❌ 情绪句/语气词 `但要你自己站在这里`、`这才是它该有的样子`
- ❌ 感叹号堆叠、`⚠` 与「：」套用
- ❌ 一句话里两个以上数字/单位混排（拆行）

### 3.2 Before / After（真实字符串，照这个标准改全量）

| 位置 | Before | After |
|---|---|---|
| 帮助行 | `左键移动 · E 交互/施工 · 空格 光爆 · B 建造（面板分类选块） · N 夜行 · T 研究 · C 图鉴 · R 灯光 · P 暂停 · Esc 菜单` | `WASD 移动 · 左键 前往 / 按住跟随 · E 交互 · 空格 光爆 · B 建造 · T 研究 · C 图鉴 · N 夜行 · P 暂停`（两行，第二行 `Q 切换方块 · 滚轮 缩放 · Alt+左键 拆除 · H 帮助`） |
| 建造提示 | `建造中：灯柱（辉髓2 藤木2· 工期 8 工） · 左键放置工地 · Esc 取消` | `灯柱 · 辉髓2 藤木2 · 工期 8 · 按住左键连放 · Esc 取消` |
| 熔炉尾注 | `研究「自动熔炉」后可建无人值守的那一台 —— 同一套配方，代价是得持续喂火种` | `研究「自动熔炉」→ 无人值守版（需火种）` |
| 手做提示 | `按住 E 连续做（每 0.50s 一批）· 火烧得越旺越快，但要你自己站在这里` | `按住 E 连做 · 0.5 秒/批 · 火越旺越快` |
| 火种尾注 | `火种不是燃料专属 —— 藤木能凑合、木炭最划算、燃料火力最猛（烧的是光的血）` | `藤木 便宜耐烧差 · 木炭 均衡 · 燃料 最快` |
| 冷炉提示 | `⚠ 炉子冷着：走到炉边按一下 E 就是点火（藤木/木炭/燃料都能烧）` | `没火 · 按 E 点火` |
| 库存行 | `辉髓 42（扣的是容器里的料，成品也进容器）` | `辉髓 42`（"扣容器"改为 tooltip 一行小字） |
| 仓储侧栏 | `本层 71/480 · 总 71/504` | `仓储 71/480`（总容量进 tooltip；本层满 → 红色 + `!`） |
| 断粮提示 | `拓荒队断粮了！建幽菌田（7）并在光照下等它成熟` | `断粮 · 7 建幽菌田（需光照）` |
| 残页提示 | `残页：熔渊手记 · 炉语` | 保留（**短、具体、有物件感** —— 这是好文案的样板） |
| 研究节点 | `解锁「自动熔炉」：火种可选，自己从容器取料出货（拓荒者会来添火）` | `自动熔炉：自动取料出货 · 拓荒者添火` |
| 知识线索 | `夜枭从头顶掠过五次之后，你大概就知道怎么把绳子挂上天` | 保留（**这是"人味"文案，不是 AI 味** —— 区别在于它有画面感且不解释系统） |

> 判据：改完后 `grep -c '——'` 在**玩家可见字符串**里为 0。

### 3.3 术语表（统一，写进 `docs/COPY.md`）

| 统一用 | 不要用 | 备注 |
|---|---|---|
| **辉髓** | 辉髄 / 矿石 / 矿 | 现在 `worker.js`、`interact.js`、GUIDE、README 混用 |
| 藤木 | 木材 / 木头 | |
| 燃料 | 油 / 燃油 | 侧栏曾用「耗油」→ 改「燃耗」 |
| 木炭 | 炭 | |
| 火种 | 引火物 / 燃料（当火种时） | 明确"火种 ≠ 燃料" |
| 拓荒者 | 拓荒者 / 工人 / NPC | 现在混用「拓荒者/拓荒队/工人」→ 统一「拓荒者（队）」 |
| 蚀兽 | 怪物 / 敌人 | |
| 蚀痕 | 腐蚀 / blight | |
| 光爆 | 技能 / 大招 | |
| 档案 | 点数 / data | 「档案点数」首次出现后简称「档案」 |

### 3.4 面板结构收敛

- tab 条：**只留 4 个**（建造 / 研究 / 图鉴 / 夜行）→ 其余站台面板改为**靠近时浮出**（见 2.2）。
- 面板统一骨架：`标题 + 副标题（一句话）+ 内容（可滚动）+ 底部操作行`；所有列表行统一高度、左对齐、数字右对齐（等宽）。
- **面板内搜索** ✅ **W13-L 已做，但方案换了**（实现在建造 / 研究 / 图鉴 / 容器四个面板）：
  - 初稿写的是“图鉴按已解锁/未解锁、容器按材料/工具”的**分面筛选**；实际改成**一行通用文本搜索**（匹配名称 / 描述 / 分类 / 弱点）。理由：分面筛选要先定义好每一面，内容一变就得重定义；而“敲两个字就找到它”对玩家是同一件事，却不用维护任何分类表。
  - 搜索是**只切可见性、不重建面板**（重建会丢输入焦点、取消滚动条拖动、把滚动顶回顶部）—— 面板重建只用于结构变化，重建后自动把查询重新盖上去。
  - 研究面板是分区的，所以额外给了**跨分区命中**（当前页搜不到时列出别的分区的命中，那串就是分区 chip，点一下切过去）。
  - 不剧透：没揭开的分区 / 没解锁的图鉴条目不参与匹配。
  - 反向记录：**容器面板的筛选与「行不跟着数量跳」不矛盾** —— 筛选是筛选，行序（`RES_ORDER`）始终不变。
- 滚动记忆已有 ✅（`scrollMemo`），保留。

### 3.5 存储面板重做（用户点名）

```
现在：[容器] 辉髓 0 │ 藤木 0 │ 石头 0 │ … 14 行 × 2 栏（空行也占位）
改为：箱子里有什么就列什么，按数量降序；空 → 一句话
```

- ✅ **修正（W13-G，用户点名）**：排序**改回按物品 ID 的固定次序**（`RES_ORDER` / `TOOL_ORDER`），**不再按数量降序**。理由：连点「取 1 / 全」时数量一直在变，按数量排会让整列行号每点一下重排一次，玩家下一击就打偏。
  同理：**同一趟面板里露过面的行，归零后留在原地变灰**（`sbtnx.off` 不响应点击）而不是消失让下面的行往上顶；关掉重开才算一趟结束（`_storeSeen` 每趟重算）。

- 左栏（容器）/ 右栏（背包）**只列 `> 0` 的条目**；空栏显示 `空 · 按 E 继续搬运`。
- 每行：`图标 名称 ×N` + 悬停显示操作 ±（Shift = 10、Ctrl = 全部）。
- 顶部一条**容量条**（`71/240`）+ 剩余格数；满仓变红并提示"再采会丢"。
- 工具单独分节（`工具` 小标题），与材料分开——因为它们是"物品"不是"资源"。
- **拖拽搬运** ✅（W13-J 已落地，与初稿不同，以下为实际实现）：左右栏**拖一行到对面那一栏 = 整摞搬过去**（跟 MC 一致）；**没有** Shift+拖 = 全 这种修饰键——「搬一个」已经有行上的「取1 / 存1」，再给拖拽挂修饰键只是让玩家多背一条规则。
  - 同栏不放（`dragover` 不 `preventDefault` → 光标显示禁止）；落点那一栏整栏虚线高亮（`.scol.drop`），源行虚线变淡（`.srow.dragging`）。
  - 搬不动时（对面满）提示「背包满了，搬不过去」而不是默默无反应。
  - ⚠ 实现坑：`panels.js` **不持有 `state`**（所有函数都是参数传入），拖拽处理器必须由 `main.js` 传 `getState` 取当前局（否则落点直接 `ReferenceError`）。

### 3.6 侧栏瘦身

- 分组：**材料**（常用 4 + `更多 ▾`）/ **营地**（光压、燃耗、蚀痕、仓储）/ **队伍**（名册）。
- 材料行：仅在"本层"数量变化时短暂高亮（0.6s 淡出）——让玩家看到"进账"。
- 名册单行化：`名字 · HP 条 · 状态图标 · 心情图标`，悬停出详情（已有 `#wtip` ✅）。
- 蚀痕/仓储加**临界状态**：蚀痕 > 30 格 → 黄，> 100 → 红。
- ✅ **三段折叠（W13-J，用户点名）**：材料 / 营地 / 拓荒队三个标题行都能点开合（`state` 无关，状态存 `localStorage['deep-light-ui-v1']` 的 `{fold:{res,camp,roster}}`；旧的 `{rosterCollapsed}` 自动迁移）。
  - 统一成一套机制：`foldHead(which,title)` + `foldBody(which,inner)` + `toggleFold(which)`；G 键保留（= `toggleFold('roster')`），旧的 `#sidebar.collapsed .wbody` 规则删除，改成通用的 `.sb-body.off { display:none }`。
  - 折叠时 `refreshResLive()` 照旧原位刷数字（元素还在 DOM 里，只是 `display:none`），展开即是最新值。

### 3.7 视觉规范（"美观"落地成数字）

| 项 | 规范 |
|---|---|
| 调色板 | 收进 CSS 变量：`--bg / --panel / --ink / --ink-dim / --accent(紫) / --warn / --ok / --danger`；游戏内 Canvas 侧也建一份 `data/palette.js` 保持一致 |
| 间距 | 8px 网格（4 / 8 / 12 / 16 / 24） |
| 字号 | 11（辅助）/ 12（正文）/ 13（标题）/ 16（面板标题）；数字用等宽 |
| 圆角/描边 | 统一 4px 圆角、1px 描边、内阴影 1 档 |
| 图标 | 一套 inline SVG（≈20 个：辉髓/藤木/石头/木炭/燃料/食物/档案/母髓/夜髓/生命/心志/仓储/灯/塔/火/工具×4），16×16 视觉框，单色 `currentColor` |
| 状态色语义 | 可做（绿）/ 不可做（红）/ 已完成（青）/ 锁定（灰）/ 知识（紫）—— **颜色 + 形状/图标双编码**（色盲友好） |
| 动效 | ≤150ms；淡入用 opacity，不用位移；⚠ 类抖动 ≤3 帧 |
| 对比度 | 正文 ≥ 4.5:1（现在 `#6d7787` on `#14161c` 只有 ~3.1:1 → 提亮到 `#8b93a6`） |
| 昼夜/蚀潮 | HUD 顶部随相位换色调（白天暖、蚀潮冷紫）——已部分实现，统一到变量 |

### 3.8 引导与"只说一次"

- 建立 `state.seen = {}` 标记：**首次**遇到某系统时给一次提示（3 秒后自动消失），之后不再打扰。
  例：第一次燃料为 0 → `燃料空了 · 采辉髓→熔炉炼；或烧藤木引火`；第二次起只在 HUD 角落亮红灯。
- 帮助行按 `H` 展开完整键位表（覆盖层，Esc 关闭）——取代常驻长条。
- 不做教程关（与"发现感"冲突），只做**一次提示 + 可查手册**。

### 3.9 实际落地（W13-E）与差异

**做了什么**

| 项 | 落地 |
|---|---|
| 调色板 | 新增 `js/data/palette.js`（`UI` / `STATE` / `RES` / `CAT` + `contrast()` 自检）；CSS 侧 `css/style.css` 顶部一套 `:root` 变量（两边一起改）。`data/storage.js` 的 `RES_COLOR` 改为直接引用 palette，**同一类东西只有一处色号** |
| 图标集 | 新增 `js/ui/icons.js`：**33 个 inline SVG**（16×16、1.5 描边、`currentColor`）×·资源 9 · 工具 4 · 属性 4 · 建筑 15 · 状态 6 · 分类 5；`icon()/resIcon()/buildIcon()` 负责取用，零外部文件/零图标库 |
| 图标接在哪 | 侧栏资源行（左侧色条 + 图标 + 名称 + 数字）、侧栏“营地”行（光压/燃耗/蚀痕/墓碑/仓储/装备/补给/光路/饵料）、快捷栏每格（图标 + **花费改成图标+数字**）、建造面板每行（键位·图标·名称·花费·状态）、研究节点、残页行、容器面板每行 |
| 状态双编码 | 可做 `✓`绿 / 不可做 `✗`红 / 已完成 `✓✓`青 / 锁定 `🔒`灰 / 知识 `◆`紫 —— **颜色 + 形状一起用**（色盲也能读）；名册异常（蚀化/心志/士气/饥饿/重伤）也给一个 12px 图标 |
| 面板骨架 | 圆角统一 4px、描边 1px、间距 8 网格（4/8/12/16）、字号 11/12/13/16、动效 ≤120ms；两处重复的“夜行面板”CSS 块删掉 |
| 建造范围预览 | `render.js` 新增 `drawBuildPreview()`：灯的光照圈、塔的射程环（+震荡塔溅射内圈）、棱镜接力圈、净化圈、补给范围；均虚线圆。**幽菌田额外显示“光照 2.1 / 需 2.5 · 太暗”**（直接读 `state.light`，与 gameplay 同源） |
| 对比度 | 正文/辅助色提亮（`--ink-dim` → `#929aac`）；**把所有“用 opacity 压暗”改成“用颜色压暗”** |

**验收：对比度自动体检**（本批的硬指标）

写了个页内脚本：遍历所有带文本的元素，把文字色 × 累积不透明度、背景色逐层合成（还分别用**亮地形 #8f9aa8** 与**黑地形 #05070c** 两种最坏背景算一遍），算 WCAG 对比度。

| 界面 | 检查元素 | <4.5:1 | 平均 |
|---|---|---|---|
| HUD + 建造面板 | 133 | **0** | 10.5 |
| 键位表覆盖层 | 190 | **0** | 10.4 |
| 设置页（控制） | 150 | **0** | 10.7 |
| 信息页 | 199 | **0** | 12.3 |
| 主菜单 | 13 | **0** | 10.2 |

> **W13-M 主菜单重做后的排版验收（补充）**：菜单改成“封面式整屏”后不再靠一个居中卡片撑排版，而是靠**方向压暗**（右侧给菜单列、上下给标题与页脚）。验收尺变成“**菜单容器高度 == 卡片高度、页脚完整可见**”，在 820×900 / 847×502 / 1000×560 / 1280×760 / 860×885 五种窗口下均为 0 溢出（修掉了项目缺全局 `border-box` 引起的 34–43px 溢出）。
| 新游戏弹窗 | 18 | **0** | 10.7 |

优化前：同一把尺子是 **19 处不达标**（最低 2.41:1），全部出在“.brow.poor / .rs.lock / .sb-res.dim / .hslot.lock”这些**靠整体 opacity 压暗**的类上；最差的是建造面板里“暂时做不了”的那几行。

**差异与取舍**

| 计划 | 实际 |
|---|---|
| “平铺色变量” | ✅ 但只把**结构性**颜色（底/线/正文/辅助/状态色）收进变量；资源色这类“语义色”放 `palette.js` 并让 storage.js 引用（CSS 里的 `.r-ore` 等保留，因为它们是 `innerHTML` 拼出来的） |
| “间距 8 网格 / 字号 11-16” | ✅ 从变量取；但没把所有 `10px` 小字号一次性拨到 11px（信息密度会爆），只在**新增/改动过**的规则里统一 |
| 图集 20 个 | 实际 33 个（建筑 15 种 + 状态 6 种都要，少一个就行不成“双编码”） |
| 建造预览“光圈/射程环” | ✅ 另加：**农田当场光照数值**（这是新手上手最容错的地方） |
| 引导 `state.seen` | ✅ **W13-I 补上了**（见 §3.10）—— 当时判断它与 W13-B/D 的提示体系重叠，所以推后；现在提示行/帮助行都已稳定，正好接上去 |
| `contain: content` / `will-change` | ❌ 未测出必要（面板重建已有签名，实测 DOM 开销不显著）—— 不做无谓的 CSS 魔法 |

**踩坑**

1. **`opacity` 是隐形的对比度杀手**：`.brow { opacity: .62 }` 看起来只是“变淡”，实际上把子元素文字色乘上了 0.62 → 4.5:1 直接掉到 2.4:1。结论：**表达“不可用”要用颜色/图标，不要用透明度**（何况透明度还会让“不可用”和“禁用”难以区分）。
2. **图标不能替代文字**：改完后侧栏最左侧是色条 + 图标，但如果只留图标，新玩家认不得“辉髓”，所以一律是 **图标 + 名称 + 数字** 三件套。
3. **图标一旦用 `currentColor`，颜色就得对**：第一批放进 `.brow` 的图标继承了行温色的 `#dfe9ff`，于是“绿色 ✓”看不出来 —— 改成 `.brow.ready .bicon { color: var(--ok) }` 才真正表达状态。
4. **新加了一列就得同步改 grid**：`.brow` 从 4 列变 5 列后忘了改 `.bdesc { grid-column: 2 / -1 }`，说明文字会错位到名字下方（已改成 `3 / -1`）。

### 3.10 实际落地（W13-I）：首次提示体系（§3.8 的完整实现）

```
计划：建立 state.seen 标记，首次遇到某系统时给一次提示（3 秒后自动消失），之后不再打扰
```

| 项 | 落地 |
|---|---|
| 表 | `js/systems/hints.js`：`HINTS` 10 条文案 + `firstTime / maybeHint / checkHints` |
| 显示 | `#firstnote` 横幅（底部竖排栈上方、居中、自动换行），**5.5 秒**后渗淡并隐藏；与提示行（`toast`）互不干扰 |
| 标记 | `state.seen = { key: true }`，**进存档**（读档不会又说一遍）；设置 → 画面 → 「新手提示 · 重置」清空重看 |
| 触发 | `firstGame` 在开局直接叫；其余 9 条每帧 `checkHints()` 试一遍，**全是 O(1) 状态判断** |
| 文案 | 照 `docs/COPY.md`：只说“你现在能做什么”，不解释数值、不写公式、不用长破折号 |

**判定为何用“状态型”而不是“事件型”**：`资源为 0` / `有人蚀化` / `上有夜辉草` 这类条件随时可查，一行就完，不需要在 8 个模块里埋”第一次发生“的钩子。代价是它们会在“刚巧满足条件”的那一刻弹出来 —— 而这正是玩家需要它的时刻（空燃料、断粮、怪已经长出来了）。

**三个设计约束**

1. **只能有一处发提示**：全部经 `maybeHint(state, key, show)`，里面同时做“是否说过”与“是否允许显示（设置里可关）”，否则很容易出现“测试时说过、玩游戏又弹一次”。
2. **不能每帧扫地图**：蚀痕那条用 `state._blightAny`（`systems/blight.js` 第一次累积时点亮的旗子），否则 hints 会变成第二个每帧扫 6912 格的模块。
3. **提示要能看回去**：一次性内容最容易变成“玩家错过了就永远不知道”，所以必须有重置入口（放在设置里，不要藏）。

**踩坑**：调试句柄自己把标记吃掉了 —— `__hint(key)` 原本先调 `firstTime` 再调 `maybeHint`，而 `maybeHint` 内部也会调 `firstTime`，于是“强制看一条”总是显示不出来（看起来像功能坏了，实际是测试工具写错）。**同一块状态只能由一个函数去“消耗”**。

---

## 4. 性能与算法（W13-C）

### 4.1 先测量，再优化

- 新增 `js/core/perf.js`：分阶段打点（`sim` / `light` / `render` / `dom`），滑动平均 60 帧；调试句柄 `__perf()` 输出表格；设置页加"显示帧耗时"开关（左上角一行小字）。
- **预算**（目标机型：2 核 2G 云主机 + 集显，60 fps = 16.6ms/帧）：
  | 阶段 | 预算 |
  |---|---|
  | sim（含 AI/波次/生产） | ≤ 3 ms |
  | light | ≤ 2 ms（且**不每帧跑**） |
  | render | ≤ 6 ms |
  | dom | ≤ 2 ms |
  | 合计 | ≤ 13 ms（留 3.6ms 余量） |

### 4.2 光照（收益最大）

| 改动 | 做法 | 预期 |
|---|---|---|
| 去掉每帧分配 | `compute()` 里 `light` 改为**模块级缓冲**（按层缓存，尺寸变化才重建）；`state.light = buf` | 省 ~1.6 MB/s 垃圾 |
| **脏标记 + 降频** | 新增 `markLightDirty(state, why)`；`compute()` 仅在 ①玩家移动 > 0.25 格 ②建筑增删/档位/燃料变化 ③相位切换 ④蚀痕净化 ⑤光源被砸 时执行；否则**每 4 帧兜底一次**（15 Hz） | 光照开销 ↓ 60~90% |
| 更省的 BFS | `spread()` 内层改**扁平四邻居展开**（`nx=x+1…` 四个 `if`，不再建数组）；`m.get()` 内联为 `tiles[ni]`；队列用 `Int32Array` 环形缓冲或复用的 `q` 数组 | 单次 BFS ↑2~5× |
| 量化 | `light` 由 `Float32Array` 改 `Uint8Array`（0~255，亮度 ×20 存），比较阈值改整数 | 内存 ↓75%，缓存友好 |

### 4.3 渲染（第二大收益）

**方案：静态烘焙 + 光罩叠加**（Minecraft 的 chunk bake 思路）

1. **地形烘焙**：地图变化时（`genMap` 后 / 挖穿一格 / 建造完成）把地形（含 `speck` 纹理、矿脉晶屑、苔斑）画到**整图离屏 canvas**（96×72×16 = 1536×1152，约 7 MB）——一次性。
2. **每帧**：`ctx.drawImage(bake, 视口)` = **1 次绘制**，取代 1,350 次 `fillRect` + 上千次 `mix()`/`speck()`/字符串拼接。
3. **光罩**：把 `light[]` 写入 **45×30 的 `ImageData`**（1 像素 = 1 格，颜色 = 黑 + α 随亮度），`putImageData` 到 45×30 离屏小画布，再 `drawImage` 最近邻放大到 720×480 —— 又 1 次绘制。蚀痕/岩浆自发光可作为附加通道。
4. **动态层**（建筑/单位/飘字/光束）继续直接画（数量小）；`mix()` 换成**预计算 LUT**（32 档亮度 → `fillStyle` 字符串数组）或直接内联 `rgb()` 计算。
5. 视口裁剪已有 ✅（`x0..x1`）；补 `imageSmoothingEnabled = false`（像素风必须）。
6. 让"挖穿一格"只重绘烘焙图的**那一格**（`bakeCtx.fillRect(tx*16, ty*16, 16, 16)`）→ 挖掘也不卡。

> 预期：**render 从 ≥10 ms 降到 ≤2 ms**，且每帧分配 ≈ 0。

### 4.4 寻路

| 改动 | 做法 |
|---|---|
| 去掉大分配 | `prev` 改模块级 `Int32Array` + **generation 戳**（同 `light.js` 的 `_dist` 技巧），避免每次 22 KB |
| 换 A* | 4 邻接 + Manhattan 启发；开阔地形展开节点数 ↓ 5~20×；用**二叉堆**（`Int32Array` 实现） |
| 常量提升 | `dirs` 提到模块级（现在是每次调用新建） |
| 每帧预算 | 每帧最多算 **4 条**完整路径，其余排队（`pendingPaths`）；工人路径为空时才请求 |
| 同目标共享 | 同一帧内多个工人去**同一目标格** → 只算一次（`Map<key, path>` 按帧清空） |
| `lineClear` 提速 | `Math.hypot` → 平方距离；采样步长 0.2 → 0.25；**Bresenham 直线**代替均分采样 |
| 路径跟随优化 | 路径点到达判定用平方距离；避免每帧重算朝向 |

### 4.5 数据层

| 改动 | 做法 |
|---|---|
| `syncRes` | 改**脏标记 + 每帧一次**（在 `simStep` 末尾）：`markResDirty()`；并且**原地更新** `state.res`（保持对象身份，避免 HUD 引用失效/触发额外重排） |
| `allContainers` | 结果**按帧缓存**（`cacheKey = layerId + buildings.length + beacons.length`），写操作复用同一数组 |
| `deposit` 排序 | 容器数 ≤ 10，排序成本可忽略 —— 但**去掉每次的 `out.push` 对象分配**：模块级数组 + 复用（`{kind,ref,cap,x,y,layerId}` 池） |
| `RES_ORDER` 循环 | 预热为常量数组，避免 `for...in`（V8 下 `for...in` 比数组遍历慢 3~5×） |

### 4.6 每帧零分配清单（目标）

| 位置 | 现在 | 改法 |
|---|---|---|
| `floaties` 清理 | `.filter()` 新数组 | 原地双指针压缩 |
| `beams` 清理 | `.filter()` | 同上 |
| `pickups` | 同上 | 同上 |
| 光照/寻路缓冲 | 每次 new | 模块级复用 + generation |
| 渲染颜色 | `mix()` 数组 | 直接算 / LUT |
| `Math.hypot` | 热路径大量 | `dist2()`（平方比较），只在需要真实距离时 `sqrt` |
| 临时对象 | `{x,y}` 点、`[[x,y]]` 邻居 | 复用/展开 |

### 4.7 DOM

- 保留两层刷新 ✅；把 tab 从 9 → 4（重建成本 ↓）；`#hotbar` 用 `data-live` 更新数字，不整块重建。
- 面板大 `innerHTML`：改为**模板 + 局部替换**（只在结构变化时重建，已有 `_panelSig`）。
- 加 `contain: content` / `will-change: transform` 到 `#panel`、`#sidebar`、`#hotbar`，减少重排范围。

### 4.8 小地图

- **脏标记增量**：`state.discovered` 变化 / `tiles` 变化 / `blight` 变化才重烘焙；否则直接用上次的 `ImageData`。
- 兜底 4 Hz（0.25s）重烘焙一次；标记层（建筑/单位/敌人）照旧每帧画（数量少）。

### 4.9 存档

- 只在**黎明 + 手动**存档（已是）；快照**稀疏化**（只存非 0 资源、只存非空容器）；`localStorage` 用量监控（> 3 MB 时警告 + 提示清理旧档）。
- 长局（> 30 天）测试：读档 < 200 ms、存档 < 100 ms。

### 4.10 验收（压力测试）

- `__stress(nEnemies, seconds, buildings)`：一次生成 n 只蚀兽 + 若干建筑（自带资源）跑指定秒数，输出分阶段耗时表；`__perf()` / `__perfTable()` / `__perf(true)` 分别取报告/取表/清零。
- 通过标准：**2 核 2G 环境 60 fps 稳定**；`light` 调用次数 ≤ 20/s；`findPath` ≤ 240/s；每帧分配 ≈ 0。
- 回归：4 天模拟零 `pageerror`（沿用现有测试流程）。

### 4.11 实际落地（W13-C）与差异

**实测（开发机，60 敌 + 19 建筑 + 12 灯；`__stress(60,6,40)`）**

| 指标 | 优化前 | 优化后 |
|---|---|---|
| `msPerStep`（模拟一步） | 1.237 ms | **0.169 ~ 0.213 ms** |
| `msPerDraw`（画一帧） | 6.346 ms | **0.301 ~ 0.305 ms** |
| 真实玩法整帧（含 DOM/HUD，vsync 144） | — | **1.19 ~ 1.46 ms**（预算占比 21%） |
| `draw.minimap` | 3.166 ms | 0.24 ms（真实走动时）/ 0.018 ms（静止） |
| `draw.tiles` | 2.264 ms | 0.05 ~ 0.16 ms |
| `sys.updateBlight` | 0.204 ms/步 | 0.012 ms/步 |
| 光照重算 | 60/s | **16 ~ 22/s** |
| `path.findPath` | — | 4.7/s（上限 240/s），avg 0.016 ms |
| 单帧最坏耗时 | 15 ms（采集时整块重烤） | **2.4 ms** |
| 堆内存（20 s 玩法） | — | 39 MB → **13 MB**（GC 能收干净） |

> 开发机 ≈ 目标机 5~8 倍性能，换到 2 核 2G + 集显约 **7~11 ms/帧**（预算 16.6 ms）✓

**实际改法与计划一致的部分**

| 计划 | 落地 |
|---|---|
| 4.1 `core/perf.js` 体检 | ✅ `pnow/pmark/pframe/perfReport/perfTable` + `__perf/__perfTable/__stress`；模拟步内逐系统打点 |
| 4.2 光照脏标记 + 缓冲复用 + 扁平四邻 + 队列复用 | ✅ `lightDirty()` 签名（玩家 ¼ 格 / 相位 / 建筑与灯态 / 墓碑 / 层 / 回声）+ **20 Hz 兜底**；`state.light` 原地复用（`fill(0)`，不让 render/gameplay 的引用脱钩）；`_queue` 用 `Int32Array` |
| 4.2 探索迷雾写入降温 | ✅ `dis[i]` 只在**真的从 0→1** 时写（原来每帧把几千个亮格重写一遍） |
| 4.3 地形烘焙 + 光罩 | ✅ 离屏地形图 + **1px=1格 光照遮罩**；`imageSmoothingEnabled=false`；见下方“数学等价推导” |
| 4.6 “挖穿一格只重绘那格” | ✅ `map.set()` 顺手记脏格（`m._dirty`），渲染侧每帧**增量重烤**（上限 1200 格/帧）；开局/切层只先烤“视野 + 10 格”，其余排队补 |
| 4.4 寻路去大分配 | ✅ `prev/gen/q` 模块级复用 + **代际戳**取代 `fill(-1)`；`dirs` 提到模块级扁平数组 |
| 4.8 小地图 | ✅ 底图**限频 8 Hz**（切层立即），标记层每帧照旧 |
| 4.10 验收 | ✅ `__stress()` 三段式；4 天（400 s）模拟回归零 `pageerror`；`overlap.worst = 0` |

**有意未做（先量后判，测出来不值得做）**

| 计划项 | 为什么不做 |
|---|---|
| A* + 二叉堆 | 实测 `findPath` 只有 **4.7 次/秒**（上限 240/s），avg 0.016 ms —— BFS 完全够用，换 A* 只增加出错面 |
| `light` 改 `Uint8Array` 量化 | 光照已降到 16~22 次/秒，`compute` 约 0.05 ms/次 → 省不下 0.1 ms，但要改动全部读取点 |
| `syncRes` 原地更新 + `allContainers` 按帧缓存 | 实测 `syncRes` **15 次/秒、avg 0.005 ms**（全场 20 s 合计 0.3 ms）—— 不是瓶颈 |
| `updateEnemies` 提 `lightTargets` | 现在 0.051 ms/步（占 4%），hoist 收益 < 0.05 ms |
| `floaties/beams/pickups` 原地压缩换掉 `.filter()` | 数组长度都在个位数～几十，单次 < 0.01 ms |
| `Math.hypot` 全面换 `dist2` | 同上；只在高频循环里各自换掉了 |
| 4.7 `contain/will-change`、tab 9→4 | 属 W13-E（面板骨架统一）范围，一起做更省事 |
| 4.9 存档稀疏化 | 与 W13-C 无关（存档是黎明一次性行为，实测读档 < 200 ms） |

**踩坑与结论**

1. **光罩的数学等价**必须写清楚，否则后面没人敢改：
   `mix(DARK, base, f) = base·f + DARK·(1−f)`。把**全亮版本**烤进地形图，再用「暗底色 × (1−f)」的遮罩叠上去，合成结果逐字节等于旧公式（实测 176 格抽样：73 格完全一致、其余偏差 ≤1/255，唯一更大的偏差来自旁边的自发光/篝火“漏光”）。
2. **细节阈值会消失**：旧代码里 `b > 0.22` 才画岩壁高光/矿脉晶屑（“亮到一定程度才看得见纹理”）。改成遮罩后细节按亮度**连续淡入**（`α·f`），视觉上更顺，但这是行为变化 —— 见 README 的对应条目。
3. **“整块重烤”会卡**：第一版把整张 1536×1152 在 `m._ver` 变化时全重画，结果采一次矿就有一帧 **15 ms**（正好卡一下）。改成脏格增量 + 每帧上限后最大 2.4 ms。
4. **地形写入必须走一条路**：新约定 —— 改地形一律 `map.set(x, y, T)`（它负责脏格入队）。原本有两处直接 `map.tiles[i] = …`，已改成调用 `map.set`。
5. **GC 会污染测量**：优化前每次 `__stress` 的数字相差 40%（6.35 → 0.30 ms），主因不是 CPU 而是**每帧 ~10 MB/s 的垃圾**（逐格字符串/`mix()` 数组/小地图 `ImageData`）让计时循环里不断撞上 GC。所以“分配 ≈ 0”不是洁癖，它直接决定低配机的帧稳定性。
6. **4 Hz 蚀痕的时间语义**：累加 `dt` 一次性结算（不是每 tick 固定 1/4 秒），所以长跑结果与每步结算等价；唯一差异是抖动阈值跨过时刻被量化到 0.25 s（对 30 s 级的计时器 = 0.8%）。
7. **小地图不能靠“脏签名”**：探索迷雾在走动时**每帧都在变**（签名一直在变→等于没限频）。最终用纯时间闸（8 Hz）+ 切层立即重烘。
8. **开局/切层首帧仍有一次“大烘焙”**：只烤视野 + 10 格（~2400 格，开发机 8~10 ms），地图其余部分按 1200 格/帧在 6 帧内补完（看不见）。属加载级一次性开销，可接受；若要再压，可把首帧范围降到“视野 + 2 格”。

### 4.12 实际落地（W13-G）与差异

> 这一批是**计划外**的：用户嫌节奏太赶，并点名“黎明不要一到白天怪全没”。

| 项 | 落地 |
|---|---|
| 一天长度 | 140 s → **400 s**（6 分 40 秒）：白天 300 / 黄昏 20 / 蚀潮 60 / 黎明 20 |
| 常量收口 | 全部集中到 `js/core/time.js`（`MORNING_SECS / DAY_END / TIDE_START / TIDE_END / DAY_SECS / DAWN_SECS / DAWN_T / DUSK_START` + `isTide / isDawn / isNight / phaseIndexOf / phaseInfo / ambientOf`） |
| 魔数清零 | 原先散在 **6 个文件**里的 `84 / 100 / 102 / 132 / 140` 全部换成常量（`music.js` `mind.js` `blight.js` `worker.js` `main.js` `panels.js`）—— 以前改天数必然漏改 |
| 黎明消解 | `waves.js`：残留蚀兽按 `maxHp / (DAWN_SECS × def.dawnFade)` 的速率持续扣血（**Boss 除外**），各按耐受力先后化完；`dawnFade` 写在 `js/data/enemies.js`（蚀芽 0.30 → 蚀壳 0.90） |
| 反馈 | 消解中 `slowT` 减速 + `dawnA = hp/maxHp` 让渲染整体淡出（`render.js` 一行 `globalAlpha`）；HUD 报“残留蚀兽 N 只正在消解”；夜间面板标题显示「黎明」 |
| 顺带 | 黎明期间**不刷怪**；夜辉草**留到天亮才凋谢**（给 20 s 抢收）；波次刷新间隔放宽（`4.2 − 潮位×0.22`，下限 1.9 s）而**同屏 cap 不动** —— 夜变长但不被数量压死 |

**验收（可复算）**：相位边界 299/301/319/321/379/381/399 s 全落在正确阶段与光照；`__steps(400.6×60)` 后**天数正好 +1**；黎明 20 s 内 5 种蚀兽依次化完（6/9/12/14/18 s，与 `dawnFade` 逐项吻合）；Boss 被排除并退场；真 rAF 跑 3 s 曲线正确；零报错、设置/存档未被写入。

**踩坑**：① 每个阶段边界都要有**相位判定函数**（`isTide/isDawn/isNight`），否则“黎明算不算夜里”这种问题会在 5 个系统里各答一次；② 面板的结构签名里原来写的是 `t < TIDE_START || t >= TIDE_END ? 'd' : 'n'`，加阶段后改成 `phaseIndexOf()`；③ `__steps(400*60)` 因浮点累加只到 399.9999… **不跨天**，测试要用 400.6。

---

## 5. 音频与素材（W13-D / W13-F）

### 5.0 图片素材：最丝的实现是“只要你交 13 张”

**世界层不做 AI 图**（原因见 `assets/README.md`）：光是活的，静态贴图不会跟着变暗。
所以素材只接**信息/叙事层**，且已全部接好 —— 你交图即生效，一张不交也能玩。

**已接入的三个消费点**

| 位置 | 素材 key | 尺寸 | 没有图时 |
|---|---|---|---|
| 主菜单背景插画 | `title_art` | 960×540 | 现有纯文字菜单（带光晕标题） |
| 图鉴·蚀兽立绘（击杀解锁后） | `codex_bud/shell/moth/owl/blind/core` | 96×96 | 纯文字条目（不占位、不破图） |
| 侧栏名册悬停 · 拓荒者半身像（按**专长**取图） | `colonist_miner/farmer/nightwatch/tinker/scholar` + `colonist_fallback` | 128×128 | 提示框不含图片 |

**机制**

- `js/core/assets.js`：异步加载、**缺文件静默降级**（不是错误）；`onAssetsChanged` 通知界面补图（只改 class/变量，**不重建 DOM** —— 重建会吐掉玩家正在点的那一下）
- `js/data/sprites.js`：唯一登记表（`key / src / pxW / pxH`），逻辑只认 key，换文件不改代码
- `assets/manifest.json`：可选的运行时覆盖（默认空）
- 调试：`__assets()` → `{ total, ok, missing, unconfigured, rows }`

**豆包提示词**已写成可直接复制的清单：**`assets/PROMPTS.md`**（13 张：插画×1 + 图鉴×6 + 半身像×6，含通用规格、负面词、逐张主体描述、交付与验收）。

> 与计划的差异：原本还要「建筑图标 32×32」「UI 资源图标 32×32」—— **W13-E 已用 inline SVG 代码画完**（跟状态色联动、放大不糊），所以这一批不用你画了；拓荒者行走小人继续程序化绘制（AI 多帧一致性差）。

### 5.1 音频架构（零素材优先）

```
js/core/audio.js     音频引擎（WebAudio 上下文 / 总线 / 并发限制 / 静音）
js/data/sfx.js       音色表（osc 类型、频率、时长、包络、随机范围、音量）
```

- **首次用户交互**（点击/按键）时 `AudioContext.resume()` 解锁（浏览器策略）。
- 总线：`master → sfx / ambient`；设置页加三条滑杆（主音量 / 音效 / 环境）。
- **避免声音爆炸**：同名音效 60 ms 内合并；同帧最多 6 个并发；距离衰减（世界音效按与玩家距离降音量，<2 格 100%，>12 格 0）。
- 零素材：全部 `OscillatorNode` + `GainNode` 包络；环境音用 `AudioBufferSourceNode` + 白噪声滤波（潮汐 = 低通噪声 + 缓慢 LFO）。

### 5.2 音效清单（按优先级）

| 优先级 | 事件 | 音色方向 |
|---|---|---|
| P0 | UI 点击 / 切换 tab / 错误 | 极短方波"嗒"（40 ms）；错误 = 低音双跳 |
| P0 | 放置 / 建成 / 拆除 | 放置 = 短促木质；建成 = 上行三音；拆除 = 噪声爆 |
| P0 | 采集每一次结算 | 别每 0.16s 都响 → **每 3 次响一次**，音高随机 ±5% |
| P0 | E 交互成功 / 失败 | 成功 = 轻"叮"；失败 = 闷"噗" |
| P1 | 光爆 / 击杀 | 低频下坠 + 噪声扫 |
| P1 | 灯加油 / 点火 / 熄火 | 火焰噪声起停 |
| P1 | 警报：存储满 / 断粮 / 大潮来临 | 两声警示（不要刺耳，用三角波） |
| P1 | Boss 出现 / 击败 | 低频轰鸣 / 长混响钟声 |
| P2 | 夜晚环境（蚀潮渐入）、岩浆近距噪声 | 噪声 + 滤波 LFO |
| P2 | 残页获得 / 安抚成功 / 蚀化 | 各自一段"音景小动机"（与叙事绑定） |

### 5.3 音频设计原则

- **像素游戏的味道：短、干、脆**（≤200 ms 的绝大多数音效）；不加混响（除 Boss）。
- 音量比：**UI(0.25) < 反馈(0.5) < 警报(0.8)**；环境音 -18 dB 打底。
- 随机音高避免"机关枪式重复疲劳"；**夜间不要把环境音做得太满**（信息优先）。
- 所有音效可单独静音（设置页三滑杆 + 一键静音 `M`）。

### 5.3.1 实际落地（W13-D）与差异

| 初稿 | 实际做法 | 为什么 |
|---|---|---|
| 音色表写成"每个音效一个函数" | **纯数据表** `SFX_DEFS`：`{ vol, dedupe, tone:[{f,f2,t,dur,at,vol}], noise:[{f,q,dur,hp}] }`，`f` 可写 `[min,max]` 随机 | 29 条音效只需要一张表 + 一个通用合成器；加音效 = 加一行数据 |
| 三总线 `master/sfx/ambient` | ✅ 一样，但**末尾加了一颗 DynamicsCompressor**（-12dB / 4:1） | 忙的时候（蚀潮+警报+击杀）同时 8 个音会削波，压缩器一劳永逸 |
| 并发上限 6 | **8**，且 `vol ≥ 0.7` 的**警报级音效不受限** | 警报被"丢掉"比轻微削波更糟 |
| 环境音（P2） | **本批未做**：`ambBus` 已接好、噪声缓冲已生成，留给 W13-E/后续一批（夜晚蚀潮渐入、岩浆近距） | 环境音要做 LFO 调度与昼夜插值，属于独立工程量 |
| 每 3 次采集响一次 | ✅ `everyN(key, n)`（状态存在 `state._sfxN`，存档不关心） | 0.35s 一次结算 → 每次响会变机关枪 |
| "失败音"分散在各处 | 采集/手做的失败集中在 `addFx()` 里按颜色判定（`#ff9d5c` = 失败） | 一处改动覆盖全部失败路径 |

> 验收：`__audio()` 可查（`ctx/ready/muted/voices/plays/drops`）；29 条音色逐条可播；真实动作链逐条计数 +1；**M 静音后 `sfx()` 直接返回 false**；滑杆改值即写总线 + localStorage；2 个游戏日回归零报错。

### 5.3.2 音乐（W13-D2）：合成的 + 你做的，两条腿走路

> 用户拍板：**音效全合成（已做完）；音乐留接口加载 mp3，由他来做。**

| 项 | 做法 |
|---|---|
| 接口 | `js/data/music.js` 的 `TRACKS`：7 首（`boss / tide / day / dusk / title / deep / clear`），只写 `base` 文件名；把文件丢进 `assets/music/` 即生效，**改文件不改代码** |
| 格式宽容 | 自动按 `.mp3 → .ogg → .wav → .m4a → .flac` 顺次试（开发服务器已补音频 MIME，也方便你在本地拿 wav 试听） |
| 零文件可用 | 缺文件 → 自动改用**合成垫音**（`MOODS` 的 calm/tense/dark：两条失谐三角波 + 低通 + 慢 LFO），设置里可关 |
| 切换 | `Boss > 蚀潮 > 深渊 > 黄昏 > 白天`，主菜单固定 `title`；**2.2 秒交叉淡化**，同曲不重启；一次性曲（`clear`）播完回落时段曲 |
| 音量 | 新增独立 `music` 总线（主/音效/音乐/环境四条滑杆 + M 静音）；暂停与主菜单把音乐压到 35% |
| 加载方式 | `<audio>` + `MediaElementSource`（流式解码，内存极小）——2 核 2G 友好 |
| 诊断 | `__music()`（`cur/want/files:ok|missing/pad`）、`__musicDir()`、`__musicTick(dt,n)` |

交付规格与自检清单写在 **[`assets/music/README.md`](assets/music/README.md)**（时长/响度 -14 LUFS/无缝循环/命名/优先级）。

### 5.5 追加项：实体碰撞体积（原计划没有，用户点名要）

- 新增 `systems/collide.js`：**圆-圆分离 + 质量分配 + 回弹速度**；对象池、零分配、确定性（重叠时用固定角度分开）
- **规则（手感优先于物理真实）**：
  - 玩家 / 拓荒者 / 蚀兽 都占体积，能互相顶开、撞上会弹开；飞行单位（夜枭）掠过不算
  - **玩家推得动别人，但别人推不动玩家**（否则一群怪能把你推着跑，直接毁掉操作）
  - 每步 3 趟分离 + 地形校验：**宁可重叠也不把人挤进墙**
- 配套：敌人 AI 加 **站定距离 0.75**（贴住就停，不再钻进玩家身体）+ 玩家攻击距离 0.65 → 0.85
- 旋钮：`REST_WORLD / REST_PLAYER / BOUNCE_DECAY / MAX_BOUNCE / PLAYER_PUSHABLE / ITERS`（都在文件头，改一个数就能调手感）
- 验收：重叠 0.74 → 5 步内 0；24 怪围死玩家位移 **0**；玩家主动推人 1.77 格；45 实体 0.164 ms/步

### 5.6 追加项：设置页分页 + 可改键（原计划没有，用户点名要）

> 用户原话：「既然都做按键绑定表了，干脆直接集成到设置里，像一般游戏那种，画面/控制/音频设置」

**做法：把"键位表"从一份静态 HTML 升级成系统的唯一数据源。**

```
js/data/keymap.js     动作表 KEY_ACTIONS（id / 分组 / 标签 / 默认键 / 提示 / fixed）+ 匹配与改键 API
   ├─ main.js               keyHit('help', e) …（不再写 e.code === 'KeyH'）
   ├─ systems/input.js      axis() 读 boundCode('up'/'down'/'left'/'right')
   ├─ ui/panels.js          PANELS 只存 action id；tab 标签显示当前绑定（J 建造）
   ├─ #keyhelp 覆盖层       toggleKeyHelp() 时由 KEY_ACTIONS 生成
   ├─ 顶部帮助行           renderHelpLine() 由绑定生成（R 暂停）
   └─ 设置 → 控制          每一行 = 动作 + 当前键按钮 + ↺ + 说明
```

**设置页重构成四个分页**：画面 / 控制 / 音频 / 存档（`.tabs/.itab` 沿用信息页那套；卡片加宽到 700px）。

**改键交互（对齐一般游戏的习惯）**：
- 点按键 → 「按任意键…」→ 按下即绑（**捕捉阶段拦一切**：游戏里那些键一个都不会漏出去）
- `Esc` / 点空白 = 取消；**纯修饰键不算绑定**（按住 Ctrl 不是要把 Ctrl 绑上去）
- **冲突自动交换**（两动作互换，绝不重复占用）；与固定键冲突则拒绝并说明
- 行内 `↺` 单项恢复；底部「全部恢复默认键位」；外层还有「全部恢复默认」（连设置一起）

**固定不可改**：数字快捷栏 1~9、`Esc`、鼠标四件事（只展示）—— 它们是"语义"不是"习惯"。

**踩坑**：`DEFAULTS.keys = {}` 是嵌套对象，`Object.assign(settings, DEFAULTS)` 会让两边**共享同一个对象**，于是改键会污染默认表 → `loadSettings/resetSettings` 里必须显式 `settings.keys = {}` 克隆。

### 5.4 素材管线（图片）

```
assets/
  manifest.json        {"spr_player": {"src": "img/player.png", "w":16,"h":16, "fallback":"proc:player"}}
  img/*.png            图集切片（建议单张 spritesheet）
  CREDITS.md           来源与授权登记
js/core/assets.js      预加载 + 失败回退到程序绘制 + 进度回调
```

- **回退机制是关键**：任何图缺了/加载失败 → 自动用现有 `fillRect` 程序绘制（游戏永不因缺图而崩）。
- 单个 spritesheet（≤512 KB）+ `drawImage` 切片，避免 N 个请求；载入时显示进度条（或首屏同步加载，因为只有一张）。
- 命名：`spr_<类别>_<名>_<态>.png`（`spr_lamp_lit`、`spr_worker_idle`）。
- **保留程序绘制**作为"低配模式"（设置里可切）——也方便你继续用代码画。

### 5.5 像素规范与替换清单

| 项 | 规范 |
|---|---|
| 基准 | 1 格 = 16×16 px；角色 12×14（脚踩格中心）；建筑填满 16×16 留 1px 边距 |
| 描边 | 1 px 暗色描边（提高暗底可读性） |
| 调色 | 单图 ≤ 16 色，取全局调色板子集 |
| 朝向 | 角色 4 或 8 向（先 4 向 + 水平翻转）；蚀兽 2~3 帧动画 |

**首批替换清单**（按"看得最久 → 最少"排序）

1. **玩家 + 拓荒者**（4 向待机/行走 2 帧 + 死亡 1 帧）
2. **蚀兽**：蚀芽 / 蚀壳 / 噬光虫 / 夜枭 / 蚀巢核心 / 盲蚀兽
3. **地形**：苔地 / 岩壁 / 辉髓矿 / 藤木 / 遗迹碑 / 辉髓母脉 / 岩浆（可平铺 tile）
4. **建筑**：营地篝火 / 灯柱 / 净光柱 / 木墙 / 熔炉 / 自动熔炉 / 制造台 / 解析台 / 储物箱 / 塔×2 / 竖井 / 棱镜 / 诱饵灯 / 补给站 / 幽菌田（生长 3 阶）
5. **UI 图标**（见 3.7）

### 5.6 来源建议（合规）

| 来源 | 适合 | 注意 |
|---|---|---|
| **自制**（Aseprite） | 主角/招牌建筑 | 最贴风格，但最慢 |
| **CC0 素材**（Kenney / OpenGameArt / itch.io 免费包） | 地形图块、UI 图标 | 必须登记来源；风格易不统一 |
| **AI 生成**（像素风提示词 + 后处理） | 概念先行、临时替换 | 注意生成一致性（同一角色多帧难）；务必登记 |
| 现有项目复用（你的 `ultra_math` PNG 经验） | 加载与回退管线 | 可先拿它验证 `assets.js` |

> 建议：**先做 1 张 spritesheet 只替换"玩家 + 4 只蚀兽 + 辉髓矿"**，跑通管线并确认风格，再批量替换 —— 不要一次性替换全部（风格不统一会很难看）。

### 5.7 实际落地（W13-F）与差异

**落地**

| 项 | 实际 |
|---|---|
| 清单与登记 | `js/data/sprites.js` 是**唯一登记表**（13 项：`title_art` 960×540 + 6 张图鉴立绘 96×96 + 6 张拓荒者半身像 128×128）；逻辑只认 key，不认文件名 |
| 加载器 | `js/core/assets.js`：异步、**失败静默降级**、`loadAssets()` 不阻塞启动；`hasAsset/specOf/assetStats/onAssetsChanged` |
| 三处接入 | 主菜单插画（`--art` 背景）/ 图鉴立绘（需击杀达标解锁）/ 侧栏名册悬停半身像 —— 均带回退（缺图→程序绘制/SVG，**不会破图**） |
| 晚到只补 class | `applyArt()` 只改 class 与 CSS 变量，**绝不重建 DOM**（重建会吞掉玩家正在点的那一下） |
| 提词单 | `assets/PROMPTS.md`：13 张图逐张的主题描述 + 通用规格 + 负面提词 + 交付与自检步骤 |
| 自检 | 控制台 `__assets()` → `ok / missing / unconfigured` 三态 + 逐项来源 |

**与计划的差异**

| 计划 | 实际 |
|---|---|
| 单张 spritesheet + `drawImage` 切片 | 改为**逐文件 + manifest**：13 张图各自独立，先交哪张就上哪张（spritesheet 需要工具切图，对“一张一张画”的人不方便） |
| `spr_<类别>_<名>_<态>.png` 命名 | 改为语义 key（`colonist_miner` / `codex_owl`），文件名自由，对不上就当作缺图 |
| 地形/角色/生物也换图 | ❌ **世界层继续程序绘制**：光系统是活的，静态贴图不会跟着变暗，会直接毁掉“光决定可见性”这条核心规则 |
| 建筑/UI 图标用图 | ❌ 已由 W13-E 用 **33 个 inline SVG** 解决（零外部文件、`currentColor` 能变色） |

**踩坑**

1. **自定义属性里的相对 `url()` 不是按文档解析的**：`--art: url('assets/sprites/title_art.png')` 写在**内联 style** 上，但被 `css/screens.css` 的 `background-image: var(--art)` 取用时，Chromium 按**使用它的那张样式表**解析 → 请求变成 `/css/assets/sprites/title_art.png` → **404**。
   后果：图明明交了、`__assets()` 也显示 `ok`（那是 JS `Image()` 预加载的另一条路），但**菜单上根本看不到**。修法：`js/ui/screens.js` 统一走 `new URL(art.src, document.baseURI).href`（绝对 URL），已用 `performance.getEntriesByName()` 确认 `status 200`。
2. **素材晚到重建 DOM 会吞掉点击**：Playwright 报了 “element intercepts pointer events” —— 玩家正戳在那个按钮上时整块 DOM 被换掉，这一下就丢了。结论：**热替换只改 class / CSS 变量**。
3. **`ok` 不等于“看得见”**：自检要看到像素（`getImageData`/截图）或实际请求（`performance` 条目），不然“加载成功但没显示”这种错会一直在。

### 5.8 实际落地（W13-H）与差异

> 计划外的一批：用户会编曲，问“需要哪些音效/音乐”。结论是先把**插座**装好，不然交上来的素材没地方放。

| 通道 | 落地 | 缺文件时 |
|---|---|---|
| 音乐（7 首） | 沿用 `assets/music/`：**文件名即接口**，`js/data/music.js` 的 `TRACKS` 驱动 | 合成垫音（按 calm/tense/dark 三种情绪） |
| 音效（30 条） | 新增 `js/core/sfxbank.js` + `assets/sfx/manifest.json`：`{"sounds":{"id":"file"}}` → `decodeAudioData` → 存 AudioBuffer | 用 `js/data/sfx.js` 的现场合成音 |
| 环境音（3 床） | 新增 `js/core/ambient.js` + `assets/ambient/manifest.json`：`night`（黄昏淡入→蚀潮满→黎明 35%）/ `deep`（玩家在地面以下）/ `fire`（最近燃烧建筑 4→14 格衰减），loop 源常驻 + 逐帧增益逼近 | 该床静音，其它不受影响 |

**关键设计**：`audio.js` 的 `sfx()` 变成**文件优先、合成兜底**，而 `dedupe / 距离衰减 / 并发上限 / 音量配平` **全部沿用 `SFX_DEFS`** —— 所以换文件不改混音平衡，作曲的人不用猜“这条该多大声”。

**顺手修的三件事**

1. **三处“定义了但从来没响”的音效**：`shoot`（塔开火 —— 之前夜里几座塔开火是**完全静音**的）、`soothe`（安抚蚀化者）、`extinguish`（熄火）。
2. **混音修正**：没有音乐文件时的合成垫音原来挂在 **ambient 总线**上（于是「音乐」滑杆管不到它、「环境音」滑杆却被它占着），已改挂 **music 总线**。
3. **交付清单**：`assets/SOUND.md`（音乐 7 + 音效 30 + 环境音 3 + **一份“还没有声音的地方”**）；图与声音的清单分别是 `assets/PROMPTS.md` 与 `assets/SOUND.md`。

**踩坑**：① 音效不能只有“合成”一条路 —— 合成音上限是“有反应”，真实的打击感/材质感必须靠文件，所以**先做接口再要素材**；② 环境音与音效的生命周期完全不同（一个常驻循环、一个一次性事件），混在用一套 API 里会两头别扭；③ 垫音的**调性要公开**（calm=A1 / tense=G1 / dark=E1），不然“垫音淡出→曲子淡入”会跳调。


---

## 6. 分批落地计划与验收

| 批次 | 内容 | 验收判据 | 主要文件 |
|---|---|---|---|
| **W13-B** 文案与信息架构 | 文案三原则 + 术语统一（辉髓）+ 帮助行重做 + 提示行收敛 + 存储面板只列非空 + 侧栏分组 + tab 收敛 | 玩家可见文案 `——` 计数 0；存储空栏显示一句话；帮助行含 WASD；改完 4 天回归零报错 | `index.html` `panels.js` `inspect.js` `main.js` `style.css` `docs/COPY.md` |
| **W13-A** 操作手感 ✅ | 按住左键跟随 / 右键上下文 / 滚轮 / 中键 / Alt 拆 / 快捷栏 + Q / Shift 直线 / Ctrl+Z 撤销 / Tab 切面板 / 误触保护 | 建 10 面墙 ≤ 3 次操作；拆除 1 步；主菜单键位表与实现一致；`__keys()` 输出映射表用于核对 | `input.js` `main.js` `building.js` `panels.js` `index.html` |
| **W13-D** 音效 ✅ | `core/audio.js` + `data/sfx.js` + 设置三滑杆 + P0/P1 音效 + 距离衰减与合并 | 无漏音/爆音；静音后无 CPU 占用；首次交互才创建 AudioContext | `audio.js`(新) `sfx.js`(新) `settings.js` `main.js` `screens.js` |
| **W13-C** 性能 ✅ | `core/perf.js` → 光照脏标记/复用缓冲 → 渲染烘焙+光罩 → 寻路代际戳 → 小地图限频 | `__stress(60,6,40)`：msPerStep 1.237→**0.17**、msPerDraw 6.35→**0.30**；真实玩法整帧 **1.2~1.5 ms**（预算 21%）；light **16~22/s**；findPath 4.7/s；堆 39→13 MB；4 天回归零报错 | `perf.js`(新) `light.js` `render.js` `minimap.js` `pathfinding.js` `blight.js` `map.js` `main.js` |
| **W13-E** 视觉 ✅ | CSS/JS 双调色板 + 33 个 inline SVG 图标 + 图标接入侧栏/快捷栏/建造/研究/容器 + 状态双编码 + 建造范围预览 + 对比度修复 | 6 个界面共 683 个文本元素，**<4.5:1 的 0 处**（优化前 19 处，最低 2.41）；平均对比度 10.2~12.3；真实键鼠回归零报错 | `palette.js`(新) `icons.js`(新) `style.css` `screens.css` `main.js` `panels.js` `render.js` `storage.js` |
| **W13-F** 素材接入 ✅管线 | `core/assets.js` + `data/sprites.js` + `assets/manifest.json` + 三处接入（菜单插画/图鉴立绘/拓荒者半身像）+ `__assets()` + 豆包提示词清单 | 测试图验证：3 张各自出现在应出现的位置；**全无素材时零破图、零报错**（`ok:0 / missing:13`），1 游戏日回归正常 | `assets.js`(新) `sprites.js`(新) `manifest.json`(新) `PROMPTS.md`(新) `main.js` `panels.js` `inspect.js` `screens.js` `style.css` `screens.css` |

**每批的共同要求**（沿用你的既有流程）：真实键盘/鼠标验收 + `__steps` 确定性回归 + 文档同步（README/GUIDE/DESIGN）+ 记忆落档 + 零 `pageerror`。

---

## 7. 需要你拍板的 7 个点（→ 已全部按推荐默认执行）

> 当时问卷没答上来，所以按下面的默认值推进了；事后逐条回看都成立，本节点可视为**已关闭**。

1. **施工顺序**：B → A → D → C → E → F ✅ 已按此执行（B/A/D/C/E/F 全部落地）。
2. **快捷栏**：数字键**关面板也生效** ✅（MC 习惯；实测误触可忽略，而且 Q 换类 + 面板内高亮能纠回）。
3. **右键职责**：改为**泰拉式“对光标处做事”** ✅（好处远大于重学成本：采集/加油/施工/开面板全一个键）。
4. **Shift 快走**：**未做**（保持“光/恐惧”的节奏，移动速度不另开维度）。
5. **音效路线**：**全部 WebAudio 合成** ✅（零素材、当天可上）；后续另开文件通道（W13-H）。
6. **素材来源**：**自制 + AI 生成提词**（`assets/PROMPTS.md`）✅；世界层保持程序绘制。
7. **性能目标机型**：**2 核 2G 阿里云 ECS + 集显** ✅（以此为验收下限，实测整帧 1.2~1.5 ms）。

---

## 8. 明确不建议做的事（守住范围）

| 不做 | 原因 |
|---|---|
| 引入 Vite/TS/框架 | 破坏"零构建、直接开 index.html 就能玩"的优势 |
| 物品栏格子化（格子拖拽的背包） | 与现有"计数式容器"冲突，收益小于成本（环世界也不是格子背包） |
| 2×2 多格建筑 | 会让寻路/占格/存档全部复杂化，收益有限 |
| 快捷键完全自定义 | 先固定一套合理的，等系统稳定再考虑 |
| 每天自动存档改为更频繁 | 会放大存档开销（4.9 才刚优化） |
| 为"好看"加大量粒子/发光 | 与暗色调性冲突，且低配机掉帧（用音效与动效补） |
| 一次替换全部图片素材 | 风格不统一会立刻变丑（先做 1 张图集验证） |

---

## 附：本方案的代码证据索引

| 结论 | 证据位置 |
|---|---|
| 光照每步全量重算 + 每帧分配 | `js/world/light.js` `compute()`（`new Float32Array(n)`）、`spread()` 内层 `const nb = [[…]]` |
| 单帧最多补算 15 步 | `js/main.js` `MAX_ACC = 0.25` / `STEP = 1/60` / `while (acc >= STEP)` |
| 渲染每格分配 | `js/systems/render.js` `mix()` 返回新数组、`speck()` 每次 `Math.sin`、逐格 `fillStyle` 字符串 |
| 寻路每次 22 KB 分配 | `js/world/pathfinding.js` `findPath()` 的 `new Int32Array(w * h)` |
| 账本每次写操作全量重算 | `js/systems/storage.js` `syncRes()` 新建对象替换 `state.res`、`allContainers()` 新建数组、`deposit()` 内 `sort` |
| 小地图每帧 6,912 像素重写 | `js/systems/minimap.js` `drawMinimap()` 尾部循环 + `putImageData` |
| 鼠标只有 click/contextmenu | `js/main.js` 只有 `addEventListener('click' | 'contextmenu')`，无 `mousedown/mouseup/wheel` |
| WASD 已实现但未写进帮助 | `js/systems/input.js` `MAP` + `js/main.js` `moveManual` vs `index.html` `#help` |
| 术语混用 | `js/entities/worker.js:370`、`js/systems/interact.js:146`（`辉髄`）vs 其余文件（`辉髓`） |
| 存储面板列全部物品 | `js/ui/panels.js` `storeCol()` 遍历 `STORE_ORDER` |
| 无音频系统 | 全项目无 `AudioContext` 命中 |
